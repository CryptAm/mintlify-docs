---
title: 'Начало работы с чат-агентами'
description: 'Пошаговое руководство по созданию, тестированию и развёртыванию вашего первого агента для обмена сообщениями на XMTP'
sidebarTitle: 'Начало работы'
---

Создавайте мощных чат-агентов, которые бесшовно интегрируются с Base App с использованием протокола обмена сообщениями XMTP.

<Note>
Полное руководство доступно в [документации XMTP](https://docs.xmtp.org/agents/get-started/build-an-agent)
</Note>

## Установка

<CodeGroup>

```bash npm
npm install @xmtp/agent-sdk
```

```bash pnpm
pnpm add @xmtp/agent-sdk
```

```bash yarn
yarn add @xmtp/agent-sdk
```

</CodeGroup>

## Использование

Этот пример показывает, как создать агента, который отправляет сообщение при получении текстового сообщения.

```ts [Node]
import { Agent } from '@xmtp/agent-sdk';

// 2. Запуск агента
const agent = await Agent.createFromEnv({
  env: 'production', // Base app работает только в production
});

// 3. Ответ на текстовые сообщения
agent.on('text', async (ctx) => {
  await ctx.sendText('Hello from my Base App Agent! 👋');
});

// 4. Логирование готовности
agent.on('start', () => {
  console.log(`Waiting for messages...`);
  console.log(`Address: ${agent.address}`);
});

await agent.start();
```

### Настройка переменных окружения

Для запуска примера агента XMTP вы должны создать файл `.env` со следующими переменными:

```bash
XMTP_WALLET_KEY= # приватный ключ кошелька
XMTP_DB_ENCRYPTION_KEY= # ключ шифрования для локальной базы данных
XMTP_ENV=production # local, dev, production
```

## Получите basename для вашего агента

Дайте вашему агенту читаемое человеком имя:

**1. Импортируйте кошелёк агента в расширение Base App:**

  • Установите браузерное расширение Base App

  • Импортируйте, используя приватный ключ вашего агента

**2. Приобретите basename:**

• Посетите https://base.org/names

• Подключите кошелёк вашего агента

• Найдите и приобретите желаемый basename (например, myagent.base.eth)

• Установите в качестве основного имени

**3. Проверьте настройку:**  	

• Теперь до вашего агента можно обращаться через basename вместо длинного адреса

• Пользователи могут отправлять сообщения на myagent.base.eth вместо 0x123...

<Note>
Полное руководство доступно в [документации XMTP](https://docs.xmtp.org/agents/get-started/build-an-agent)
</Note>
