---
title: 'Мини-приложения и агенты'
description: 'Узнайте, как интегрировать мини-приложения с чат-агентами для создания бесшовного опыта в беседе, который повышает вовлечённость и виральность.'
sidebarTitle: 'Мини-приложения и агенты'
---

> **Что вы узнаете**  
> К концу этого руководства вы сможете:
> - Интегрировать мини-приложения с чат-агентами для бесшовного опыта в беседе
> - Делиться мини-приложениями напрямую через сообщения агентов с богатыми превью
> - Создавать вовлекающие групповые впечатления, используя агентов и мини-приложения вместе
> - Реализовать упоминания пользователей и социальные функции в опыте мини-приложений, управляемых агентами
> - Строить вирусное, конверсационное распространение мини-приложений через чат

## Почему мини-приложения и агенты вместе?

Мини-приложения могут существовать внутри бесед, позволяя друзьям играть, торговать, планировать и координироваться вместе, не покидая чат.

Это означает, что вы можете распространять виральность через естественную беседу ("попробуй это прямо здесь") наряду с обменом в более крупных сообществах. В сочетании с интеллектуальными агентами вы создаёте мощные впечатления, которые распространяются через естественную беседу.

Эта интеграция открывает уникальные модели распространения и вовлечения:

**Естественная виральность:** Моменты "попробуй это прямо здесь" распространяются органично через беседу

**Контекстное вовлечение:** Агенты могут представлять мини-приложения в идеальный момент

**Групповая координация:** Агенты организуют многопользовательские впечатления и поддерживают вовлечённость всех участников

**Постоянное присутствие:** Агенты поддерживают вовлечённость между сессиями мини-приложений

**Реальные примеры:**

  • **Групповые игры:** Агент координирует многопользовательскую викторину, объявляет победителей и ведёт счёт между сессиями 
  • **Планирование событий:** Агент делится мини-приложением для планирования, когда кто-то упоминает встречу, обрабатывает RSVP и обновления
  • **Торговые соревнования:** Агент создаёт торговые челленджи, делится таблицами лидеров и отмечает победы
  • **Социальные опросы:** Агент запускает опросы, когда нужно принять решения, подсчитывает результаты и объявляет итоги


## Как работает интеграция

### 1. Делитесь мини-приложениями в приложении

У каждого мини-приложения есть публичная ссылка, которую агенты могут отправлять прямо в чат. При размещении в беседе ссылка автоматически разворачивается в богатую карточку предпросмотра, по которой можно нажать для мгновенного запуска.


**Процесс обмена:**
1. **Агент инициирует обмен** — На основе контекста беседы или запроса пользователя
2. **Создаётся предпросмотр ссылки** — Платформа получает метаданные мини-приложения и создаёт богатый эмбед
3. **Пользователи вовлекаются** — Нажимают на превью для запуска мини-приложения прямо в беседе
4. **Агент координирует** — Продолжает вовлечение, делится обновлениями, упоминает участников

### Техническая реализация

**Базовый обмен мини-приложениями:**

```typescript
agent.on("text", async (ctx) => {
  const description = "🎯 Ready for a quick quiz challenge?";
  const appUrl = "https://your-miniapp.com/quiz";
  await ctx.sendText(`${description}\n\n${appUrl}`);
}
```

**Продвинутая интеграция с контекстом:**

```typescript
// Агент определяет контекст беседы и делится соответствующим мини-приложением
async function handleMessage(message, conversation) {
  const text = message.content.toLowerCase();
  
  if (text.includes("game") || text.includes("play")) {
    await shareMiniApp(
      conversation,
      "https://your-miniapp.com/games",
      "🎮 Let's play! Choose your game:"
    );
  } else if (text.includes("vote") || text.includes("decide")) {
    await shareMiniApp(
      conversation,
      "https://your-miniapp.com/poll",
      "🗳️ Let's settle this with a poll:"
    );
  }
}
```

## Вовлечение пользователей и упоминания

### 2. Вовлекайте и упоминайте пользователей

Агенты на XMTP имеют доступ только к 0x-адресам. Если вы создаёте групповой чат-опыт с мини-приложениями, вам захочется использовать читаемые человеком упоминания, такие как отображаемые имена (например, @jesse), для более социального и интуитивного опыта.

Этот API от Neynar даст вашему агенту доступ к этим данным:

**Получение отображаемых имён:**

```typescript
import { NeynarAPIClient } from "@neynar/nodejs-sdk";

const neynar = new NeynarAPIClient("YOUR_API_KEY");

async function getDisplayName(address) {
  try {
    const users = await neynar.lookupUserByVerification(address);
    return users.result.users[0]?.display_name || address.slice(0, 8);
  } catch (error) {
    return address.slice(0, 8); // Запасной вариант - сокращённый адрес
  }
}

// Использование в сообщениях агента
async function announceWinner(conversation, winnerAddress, gameType) {
  const displayName = await getDisplayName(winnerAddress);
  await conversation.send(`🏆 @${displayName} wins the ${gameType}! Amazing job!`);
}
```

### Создание социальных групповых впечатлений

**Пример: Координация многопользовательской игры**

```typescript
class GameAgent {
  private activeGames = new Map();
  
  async handleGameMessage(message, conversation) {
    const gameId = conversation.id;
    
    if (message.content.includes("/start")) {
      await this.startGame(conversation, gameId);
    } else if (message.content.includes("/join")) {
      await this.joinGame(conversation, gameId, message.senderAddress);
    } else if (message.content.includes("/results")) {
      await this.shareResults(conversation, gameId);
    }
  }
  
  async startGame(conversation, gameId) {
    const gameUrl = `https://your-miniapp.com/game/${gameId}`;
    this.activeGames.set(gameId, { players: [], started: Date.now() });
    
    await conversation.send(
      `🎮 New game starting! Tap to join:\n\n${gameUrl}\n\nType /join to register`
    );
  }
  
  async joinGame(conversation, gameId, playerAddress) {
    const game = this.activeGames.get(gameId);
    if (!game.players.includes(playerAddress)) {
      game.players.push(playerAddress);
      
      const displayName = await getDisplayName(playerAddress);
      await conversation.send(`✅ @${displayName} joined the game! ${game.players.length} players ready`);
    }
  }
  
  async shareResults(conversation, gameId) {
    const game = this.activeGames.get(gameId);
    const results = await fetchGameResults(gameId); // Ваш игровой API
    
    let message = "🏆 Game Results:\n\n";
    for (let i = 0; i < results.length; i++) {
      const displayName = await getDisplayName(results[i].address);
      message += `${i + 1}. @${displayName} - ${results[i].score} points\n`;
    }
    
    await conversation.send(message);
  }
}
```

## Пример агента: Squabble

[Squabble](https://github.com/builders-garden/squabble) использует мини-приложения социальным, интересным способом, с агентом, координирующим многопользовательский опыт. Изучите их репозиторий на GitHub для вдохновения!

Агент отправляет мини-приложение в группу и транслирует группе обновления по мере присоединения людей и соревнования в игре. Они упоминают имена пользователей людей, которые присоединились к игре и выиграли её. Игра происходит внутри мини-приложения, что обеспечивает более интерактивный, визуальный опыт.

### Как работает Squabble

<Steps>
<Step title="Агент инициирует игру">
  Агент определяет игровой контекст в беседе и делится ссылкой на мини-приложение Squabble, которая разворачивается в богатую карточку предпросмотра.
</Step>

<Step title="Игроки присоединяются через мини-приложение">
  Пользователи нажимают на превью, чтобы запустить мини-приложение, присоединиться к игре и начать играть в интерактивную словесную игру.
</Step>

<Step title="Агент координирует опыт">
  Агент отслеживает прогресс игры и транслирует обновления группе по мере присоединения игроков и соревнования.
</Step>

<Step title="Агент празднует результаты">
  Агент упоминает победителей по имени пользователя, делится итоговыми счетами и поощряет повторную игру для продолжения вовлечения.
</Step>
</Steps>

**Ключевые особенности:**
- **Богатые упоминания** — Агент использует отображаемые имена (@username) вместо адресов кошельков
- **Обновления в реальном времени** — Транслирует уведомления о присоединении и прогрессе игры для поддержания групповой вовлечённости
- **Социальное празднование** — Объявляет победителей и достижения для стимулирования продолжения участия
- **Контекстные триггеры** — Реагирует на игровые темы в беседе соответствующими обменами мини-приложений


## Рекомендации

### Поведение агента

<AccordionGroup>
<Accordion title="Время и контекст">
- **Умные триггеры** — Делитесь мини-приложениями, когда контекст беседы предполагает возможность вовлечения
- **Избегайте спама** — Не делитесь одним и тем же мини-приложением многократно за короткие промежутки времени
- **Чувствуйте аудиторию** — Оцените интерес группы перед введением игр или активностей
- **Естественная интеграция** — Делайте обмен мини-приложениями похожим на полезные предложения, а не рекламу
</Accordion>

<Accordion title="Групповые vs личные сообщения">
- **Личные сообщения** — Автоматически делитесь соответствующими мини-приложениями на основе запросов пользователя
- **Групповые сообщения** — Делитесь мини-приложениями только при упоминании (@имяагента) или при ответе на сообщения агента
- **Реагируйте для подтверждения** — Используйте эмодзи-реакции (👀, ⌛), чтобы показать получение сообщения во время обработки
- **Координируйте опыт** — В группах действуйте как фасилитатор для многопользовательских впечатлений с мини-приложениями
</Accordion>

<Accordion title="Пользовательский опыт">
- **Используйте отображаемые имена** — Всегда преобразовывайте адреса в читаемые человеком имена для упоминаний
- **Отмечайте достижения** — Объявляйте победы, завершения и вехи для повышения вовлечённости
- **Поддерживайте контекст** — Помните о текущих играх/активностях и предоставляйте соответствующие обновления
- **Обеспечивайте открытие** — Предлагайте связанные мини-приложения на основе интересов и поведения пользователей
</Accordion>
</AccordionGroup>

### Техническая реализация

- **Обработка ошибок** — Корректно обрабатывайте сбои загрузки мини-приложений или таймауты API
- **Ограничение частоты** — Соблюдайте лимиты сообщений XMTP и избегайте перегрузки бесед
- **Управление состоянием** — Отслеживайте активные сессии мини-приложений и участие пользователей в разных беседах
- **Конфиденциальность** — Получайте доступ только к необходимым данным пользователей и уважайте предпочтения приватности

## Руководство по реализации

### Настройка интеграции мини-приложений

<Steps>
<Step title="Настройте агента с URL мини-приложений">
  Настройте вашего агента с каталогом URL мини-приложений и связанных с ними триггеров или контекстов.
  
  ```typescript
  const miniApps = {
    games: "https://your-miniapp.com/games",
    polls: "https://your-miniapp.com/polls", 
    trading: "https://your-miniapp.com/trading",
    events: "https://your-miniapp.com/events"
  };
  ```
</Step>

<Step title="Реализуйте определение контекста">
  Создайте логику для определения, когда обмен мини-приложением будет ценным на основе содержимого беседы.
  
  ```typescript
  function detectMiniAppContext(message) {
    const content = message.content.toLowerCase();
    if (content.includes("game") || content.includes("play")) return "games";
    if (content.includes("vote") || content.includes("poll")) return "polls";
    return null;
  }
  ```
</Step>

<Step title="Добавьте возможности упоминания пользователей">
  Интегрируйтесь с API Neynar или аналогичным сервисом для преобразования адресов в отображаемые имена для лучшего UX.
</Step>

<Step title="Протестируйте интеграцию">
  Убедитесь, что предпросмотры мини-приложений отображаются корректно и координация агента работает в разных типах бесед.
</Step>
</Steps>

### Следующие шаги

Готовы построить вашу интеграцию мини-приложений и агентов?

- **Изучите пример [Squabble](https://github.com/builders-garden/squabble)** — Увидьте реальные шаблоны реализации
- **Ознакомьтесь с [руководством Chat Agents](/base-app/agents/chat-agents)** — Поймите основы агентов XMTP 
- **Изучите [документацию Mini Apps](/base-app/miniapps/mini-apps)** — Узнайте о разработке мини-приложений
- **Присоединитесь к [XMTP сообществу](https://community.xmtp.org/)** — Получите помощь и делитесь своими разработками

Комбинируя мини-приложения с интеллектуальными агентами, вы создаёте вовлекающие, вирусные впечатления, которые естественно распространяются через беседу, предоставляя богатую, интерактивную функциональность, которая возвращает пользователей снова и снова.
