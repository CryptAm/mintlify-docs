---
title: 'Создание компонента профиля Basename'
description: Узнайте, как создать компонент, который отображает ссылки на социальные сети и информацию профиля для заданного basename.
---

Ончейн-идентичности часто включают ссылки на социальные сети и информацию профиля, хранящуюся в виде текстовых записей. В этом руководстве вы научитесь создавать компонент, который получает и отображает эту информацию для заданного basename.

## Цели

К концу этого руководства вы сможете:

- Получать ENS текстовые записи для basename
- Создать React-компонент для отображения информации профиля
- Реализовать выпадающий список для показа/скрытия деталей профиля

## Предварительные требования

### React и TypeScript

Вы должны быть знакомы с React и TypeScript. Если вы новичок в этих технологиях, рекомендуем сначала ознакомиться с [официальной документацией React].

### OnchainKit

В этом руководстве используется [OnchainKit] от Coinbase. Знакомство с его основными концепциями будет полезным.

### Basename

Для этого руководства требуется basename с несколькими текстовыми записями (Github, Twitter, веб-сайт). Если у вас нет Basename, получите его [здесь](https://www.base.org/names).

## Установка текстовой записи

<Frame>
![image-basename](/images/basenames-tutorial/basename-profile-home.png)
</Frame>

Чтобы установить текстовую запись для вашего basename, начните с перехода по адресу `https://www.base.org/name/<ВАШЕ-BASE-ИМЯ>`. Подключите ваш кошелек для управления профилем, затем нажмите кнопку `Manage profile`. Добавьте одну или несколько текстовых записей, например, URL вашего X (бывший Twitter). После добавления нужных записей нажмите кнопку `Save` внизу страницы. Наконец, подтвердите и подпишите транзакцию, чтобы внести изменения в ончейн.

<Frame>
![image-basename](/images/basenames-tutorial/confirm-textrecord-update.png)
</Frame>

## Настройка окружения

Сначала клонируйте или форкните [OnchainKit app template]:

```
git clone git@github.com:coinbase/onchain-app-template.git
```

Затем установите следующие зависимости:

```bash
bun install framer-motion
bun install lucide-react
```

Создайте файл `.env`:

```
cp .env.local .env
```

Обновите содержимое файла `.env`, включив [Reown] (бывший WalletConnect) project ID и CDP API ключ:

```
# ~~~
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=<YOUR_GOOGLE_ANALYTICS_ID>

# See https://www.coinbase.com/developer-platform/products/base-node
NEXT_PUBLIC_CDP_API_KEY=<YOUR_CDP_API_KEY>

# ~~~
NEXT_PUBLIC_ENVIRONMENT=localhost

# See https://cloud.reown.com/
NEXT_PUBLIC_WC_PROJECT_ID=<YOUR_WC_PROJECT_ID>
```

## Создание файла `Client`

Создайте новый файл `client.ts` в директории `src` вашего проекта:

```typescript
import { http, createPublicClient } from 'viem';
import { mainnet } from 'viem/chains';

export const publicClient = createPublicClient({
  chain: base,
  transport: http(),
});
```

Этот клиент будет использоваться для взаимодействия с основной сетью Base.

## Создание компонента IdentityWrapper

Создайте новый файл `IdentityWrapper.tsx` в вашей директории `src/components` и импортируйте следующие пакеты:

```typescript
import React, { useState, useEffect } from 'react';
import { Avatar, Name, getName } from '@coinbase/onchainkit/identity';
import { motion } from 'framer-motion';
import { Twitter, Github, Globe } from 'lucide-react';
import { normalize } from 'viem/ens';
import { publicClient } from '../client';

// Код компонента будет здесь
```

`IdentityWrapper` будет получать и отображать информацию о социальных сетях и профиле для заданного адреса Base с использованием OnchainKit и Ethereum Name Service (ENS). Он будет запрашивать ENS-имя, связанное с адресом, и получать соответствующие текстовые записи (Twitter, GitHub и URL веб-сайта), используя указанный резолвер, сохраняя эти данные как в состоянии компонента, так и в локальном хранилище для оптимизации производительности. Компонент будет спроектирован так, чтобы представлять эту информацию в удобном для пользователя, раскрываемом формате.

## Реализация логики компонента

Добавьте следующий код в ваш файл `IdentityWrapper.tsx`:

```typescript
const IdentityWrapper: React.FC<{ address: string }> = ({ address }) => {
  const [ensText, setEnsText] = useState<{
    twitter: string | null;
    github: string | null;
    url: string | null;
  } | null>(null);

  const [isOpen, setIsOpen] = useState(false);

  const BASENAME_L2_RESOLVER_ADDRESS = '0xc6d566a56a1aff6508b41f6c90ff131615583bcd';

  useEffect(() => {
    const fetchEnsText = async () => {
      if (address) {
        try {
          const name = await getName({ chain: base, address: address });
          const normalizedAddress = normalize(name as string);
          const twitterText = await publicClient.getEnsText({
            name: normalizedAddress,
            key: 'com.twitter',
            universalResolverAddress: BASENAME_L2_RESOLVER_ADDRESS,
          });
          const githubText = await publicClient.getEnsText({
            name: normalizedAddress,
            key: 'com.github',
            universalResolverAddress: BASENAME_L2_RESOLVER_ADDRESS,
          });
          const urlText = await publicClient.getEnsText({
            name: normalizedAddress,
            key: 'url',
            universalResolverAddress: BASENAME_L2_RESOLVER_ADDRESS,
          });
          const fetchedData = {
            twitter: twitterText,
            github: githubText,
            url: urlText,
          };
          setEnsText(fetchedData);
          localStorage.setItem(address, JSON.stringify(fetchedData));
        } catch (error) {
          console.error('Error fetching ENS text:', error);
        }
      }
    };
    fetchEnsText();
  }, [address]);

  // Логика рендеринга будет здесь
};

export default IdentityWrapper;
```

<Note>
**Возможные текстовые записи для Basename:**

Полный список существующих текстовых записей для basename:

```
  Description,
  Keywords,
  Url,
  Github,
  Email,
  Phone,
  Twitter,
  Farcaster,
  Lens,
  Telegram,
  Discord,
  Avatar
```
</Note>


## Реализация логики рендеринга

Добавьте следующую логику рендеринга в ваш компонент `IdentityWrapper`:

```typescript
return (
  <>
    {address ? (
      <motion.div
        className="relative space-y-2"
        initial={{ opacity: 0, y: -20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.5 }}
      >
        <div className="flex items-center space-x-2">
          <Avatar address={address} />
          <Name address={address} />
        </div>
        <button onClick={() => setIsOpen(!isOpen)} className="text-blue-500 hover:underline">
          {isOpen ? 'Hide' : 'Show'} Profile
        </button>
        {ensText && (
          <motion.div
            className="rounded-lg bg-white bg-opacity-20 p-4 text-white shadow-md"
            initial={{ height: 0, opacity: 0 }}
            animate={{ height: isOpen ? 'auto' : 0, opacity: isOpen ? 1 : 0 }}
            transition={{ duration: 0.5, ease: 'easeInOut' }}
            style={{ overflow: 'hidden' }}
          >
            {ensText.twitter && (
              <div className="flex items-center space-x-2">
                <Twitter className="h-4 w-4" />
                <span>Twitter:</span>
                <a
                  href={`https://x.com/${ensText.twitter}`}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="hover:underline"
                >
                  {ensText.twitter}
                </a>
              </div>
            )}
            {ensText.github && (
              <div className="mt-2 flex items-center space-x-2">
                <Github className="h-4 w-4" />
                <span>Github:</span>
                <a
                  href={`https://github.com/${ensText.github}`}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="hover:underline"
                >
                  {ensText.github}
                </a>
              </div>
            )}
            {ensText.url && (
              <div className="mt-2 flex items-center space-x-2">
                <Globe className="h-4 w-4" />
                <span>Website:</span>
                <a
                  href={ensText.url}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="hover:underline"
                >
                  {ensText.url.replace(/^https?:\/\//, '')}
                </a>
              </div>
            )}
          </motion.div>
        )}
      </motion.div>
    ) : (
      <div className="text-white">Connect your wallet to view your profile card.</div>
    )}
  </>
);
```

## Использование компонента

Чтобы использовать этот компонент в вашем приложении, импортируйте его на нужную страницу или в компонент:

```typescript
import IdentityWrapper from '../components/IdentityWrapper';

// В вашей функции рендеринга:
<IdentityWrapper address={userAddress} />;
```

<Frame>
![profile-dropdown](/images/basenames-tutorial/profile-component-dropdown.png)
</Frame>

Примеры фрагментов кода доступны для:

- [Компонент IdentityWrapper](https://gist.github.com/hughescoin/5e5cd6cbfb3c6d1cada0a9d206b003c6)
- [Page.tsx](https://gist.github.com/hughescoin/49afc9e999d69d372a67186e804e693b)

## Заключение

Поздравляем! Вы создали компонент, который получает и отображает информацию профиля для заданного basename. Этот компонент можно легко интегрировать в ваше ончейн-приложение, чтобы предоставить пользователям богатое, интерактивное отображение профиля.

[OnchainKit]: https://github.com/coinbase/onchainkit
[Viem]: https://viem.sh/
[Framer Motion]: https://www.framer.com/motion/
[Lucide React]: https://lucide.dev/guide/packages/lucide-react
[React official documentation]: https://react.dev/
[OnchainKit app template]: https://github.com/coinbase/onchain-app-template
[Reown]: https://cloud.reown.com/

