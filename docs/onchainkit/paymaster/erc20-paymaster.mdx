---
title: 'Paymaster (ERC20): Настройте своего Paymaster для приема токенов ERC20'
sidebar_label: ERC20 Paymaster
---

<Accordion title="Развернуть для просмотра изображений (кликните для увеличения)">
<Frame caption="Paymaster - ERC20 Policy">
<img src="/images/paymaster/pb-paymaster-policy-erc20.png" width="700" alt="Paymaster ERC20 Policy" />
</Frame>
</Accordion>


### Биллинг

Поскольку вы получаете токены ERC20 обратно, CDP будет учитывать все переводы токенов и выставлять счет в конце месяца аналогично обычному спонсируемому потоку оплаты газа (на основе стоимости ERC20 в USD на момент получения токенов вашим кошельком). Это дает вам гибкость в решении, что делать с токенами. Стандартная сервисная комиссия Paymaster в размере 7% применяется к этому объему и учитывается в котировке ERC20, которую получают и принимают ваши пользователи.

Примечание: существующие CDP-кредиты, специфичные для Paymaster, продолжают действовать для обычного спонсируемого объема Paymaster, но не для объема Paymaster ERC20.

### Настройка вашей политики

**Чтобы позволить вашим пользователям оплачивать газ токенами ERC20, ваша политика должна быть настроена на отказ от обычного спонсирования.**

Это позволяет настраивать такие параметры, как:

- Полностью спонсировать 3 UserOperations на одного пользователя в день - любые дополнительные UserOperations потребуют от пользователей оплаты с использованием токена ERC20.
- Полностью спонсировать UserOperations на сумму до $1 в неделю - любые дополнительные UserOperations потребуют от пользователей оплаты с использованием токена ERC20.
- Никогда не спонсировать UserOperations полностью - все UserOperations потребуют от пользователей оплаты с использованием токена ERC20.

**Если вам нужен только третий вариант (всегда требовать от пользователей оплаты токеном ERC20), мы рекомендуем установить значение `Per User Address Max` равным `.01`.**

Также мы рекомендуем создать отдельный проект для тестирования ваших пользовательских потоков ERC20, чтобы это не повлияло на существующие потоки обычного спонсирования.


<Accordion title="Развернуть для просмотра изображений (кликните для увеличения)">
<Frame caption="Paymaster - ERC20 Policy Flow">
<img src="/images/paymaster/pb-paymaster-policy-erc20-flow.png" width="700" alt="Paymaster ERC20 Policy Flow" />
</Frame>
</Accordion>

### Руководство по интеграции и примеры кода

Paymaster ERC20 бесшовно интегрируется в любую существующую интеграцию с обычным спонсируемым Paymaster. Единственное отличие - вам необходимо настроить ваш смарт-кошелек на вызов `approve` для нашего контракта Paymaster, чтобы он мог тратить токены ERC20 вашего пользователя.

- Адрес контракта Paymaster (Sepolia): `0x709a4bae3db73a8e717aefca13e88512f738b27f`
- Адрес контракта Paymaster (Mainnet): `0x2FAEB0760D4230Ef2aC21496Bb4F0b47D634FD4c`

Например, код ниже почти идентичен файлу `index.js` [Paymaster quickstart guide](https://docs.cdp.coinbase.com/paymaster/docs/paymaster-bundler-qs-headless). Единственная разница в том, что мы добавили вызов функции `approve` в поле `calls`.

```
//index.js
import { http, encodeFunctionData, parseAbi } from "viem";
import { baseSepolia } from "viem/chains";
import { createBundlerClient } from "viem/account-abstraction";
import { account, client, RPC_URL } from "./config.js";
import { abi } from "./example-app-abi.js";

// Логирует детерминированный публичный адрес, сгенерированный вашим приватным ключом
console.log(`Minting nft to ${account.address}`)

// Бандлер - это специальный узел, который размещает вашу UserOperation в цепи
const bundlerClient = createBundlerClient({
  account,
  client,
  transport: http(RPC_URL),
  chain: baseSepolia,
});

// Разрешает нашему paymaster тратить токены ERC20 вашего пользователя. В примере ниже используется USDC
const tokenAddress = "0x036CbD53842c5426634e7929541eC2318f3dCF7e"
const tokenAmount = 1000000 // 1 USDC
const paymasterAddress = "0x709a4bae3db73a8e717aefca13e88512f738b27f"
const approve = {
  to: tokenAddress,
  data: encodeFunctionData({
      abi: parseAbi(["function approve(address,uint)"]),
      functionName: "approve",
      args: [paymasterAddress, tokenAmount],
  })
}

// Вызов для вашего приложения. Вам нужно будет изменить это в зависимости от ABI вашего dapp
const nftContractAddress = "0x66519FCAee1Ed65bc9e0aCc25cCD900668D3eD49"
const mintTo = {
  abi: abi,
  functionName: "mintTo",
  to: nftContractAddress,
  args: [account.address, 1],
};
const calls = [approve, mintTo]

// Увеличивает preVerificationGas (или любые другие лимиты газа) для гарантии размещения UserOperation в цепи
account.userOperation = {
  estimateGas: async (userOperation) => {
    const estimate = await bundlerClient.estimateUserOperationGas(userOperation);
    // adjust preVerification upward
    estimate.preVerificationGas = estimate.preVerificationGas * 2n;
    return estimate;
  },
};

// Подписать и отправить UserOperation
try {
  const userOpHash = await bundlerClient.sendUserOperation({
    account,
    calls,
    paymaster: true
  });

  const receipt = await bundlerClient.waitForUserOperationReceipt({
    hash: userOpHash,
  });

  console.log("✅ Transaction successfully sponsored!");
  console.log(`⛽ View sponsored UserOperation on blockscout: https://base-sepolia.blockscout.com/op/${receipt.userOpHash}`);
  console.log(`🔍 View NFT mint on basescan: https://sepolia.basescan.org/address/${account.address}`);
  process.exit()
} catch (error) {
  console.log("Error sending transaction: ", error);
  process.exit(1)
}
```

Пожалуйста, рассмотрите возможность проверки `allowance` (разрешения) токена ERC20 для вашего пользователя перед добавлением дополнительного вызова функции `approve` для ERC20. Это позволит сэкономить на комиссии газа для ваших пользователей, если нашему Paymaster уже разрешено тратить их средства.

```
// Проверяет allowance (разрешение)
const allowance = await client.readContract({
  abi: parseAbi(["function allowance(address owner, address spender) returns (uint256)"]),
  address: tokenAddress,
  functionName: "allowance",
  args: [account.address, paymasterAddress],
})

const calls = [mintTo]
if (allowance < tokenAmount) {
   calls.unshift(approve)
}
```

### Спецификация

CDP Paymaster ERC20 использует JSON-RPC запросы `pm_getPaymasterStubData` и `pm_getPaymasterData`, указанные в ERC-7677, а также дополнительный API `pm_getAcceptedPaymentTokens`. Мы рассмотрим каждый запрос и ответ ниже.

#### pm_getPaymasterStubData and pm_getPaymasterData

1. Paymaster будет использовать указанный токен ERC20 для оплаты, если он указан в поле контекста `erc20` стандарта 7677.
2. При отклонении / неудаче paymaster должен вернуть поле `data` в ответе JSONRPC, которое может быть использовано для одобрения paymaster и включает:

- Массив `acceptedTokens`, который является структурой, включающей адрес токена.
- Поле `paymasterAddress`, которое является адресом paymaster, выполняющего перевод токенов.

3. При успехе paymaster вернет поле `tokenPayment` в результате. Оно включает:

- `tokenAddress` - адрес токена, используемого для оплаты.
- `maxFee` - максимальная комиссия для отображения в UI.
- `decimals` - количество десятичных знаков для использования в UI.
- `name` - название токена.

Смарт-кошелек проведет симуляцию транзакции для обеспечения успешности и точности информации.

##### Запрос

Это пример стандартного запроса Entrypoint V0.6 с дополнительным контекстом для указания используемого токена.

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "pm_getPaymasterData",
  "params": [
    {
      "sender": "0xe62B4aD6A7c079F47D77a9b939D5DC67A0dcdC2B",
      "nonce": "0x4e",
      "initCode": "0x",
      "callData": "",
      "callGasLimit": "0x113e10",
      "verificationGasLimit": "0x113e10",
      "preVerificationGas": "0x113e10",
      "maxFeePerGas": "0x113e10",
      "maxPriorityFeePerGas": "0x113e10",
      "paymasterAndData": "0x",
      "signature": "0x5ee079a5dec73fe39c1ce323955fb1158fc1b9a6b2ddbec104cd5cfec740fa5531584f098b0ca95331b6e316bd76091e3ab75a7bc17c12488664d27caf19197e1c"
    },
    "0x5FF137D4b0FDCD49DcA30c7CF57E578a026d2789",
    "0x2105",
    {
      "erc20": "0x833589fcd6edb6e08f4c7c32d4f71b54bda02913"
    }
  ]
}
```

##### Ответ

Успешный ответ:

```json
{
  "id": 1,
  "jsonrpc": "2.0",
  "result": {
    "paymasterAndData": "",
    "tokenPayment": {
      "name": "USDC",
      "address": "0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913",
      "maxFee": "0xa7c8",
      "decimals": 6
    }
  }
}
```

Ответ с отклонением:

```json
{
  "id": 1,
  "jsonrpc": "2.0",
  "error": {
    "code": -32002,
    "message": "request denied - no sponsorship and address can not pay with accepted token",
    "data": {
      "acceptedTokens": [
        {
          "name": "USDC",
          "address": "0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913"
        }
      ]
    }
  }
}
```

#### pm_getAcceptedPaymentTokens

`pm_getAcceptedPaymentTokens` возвращает массив токенов, которые paymaster принимает для оплаты.
Запрос содержит entrypoint, идентификатор цепи и необязательный контекст.

##### Запрос

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "pm_getAcceptedPaymentTokens",
  "params": ["0x5FF137D4b0FDCD49DcA30c7CF57E578a026d2789", "0x2105", {}]
}
```

##### Ответ

```json
{
  "id": 1,
  "jsonrpc": "2.0",
  "result": {
    "acceptedTokens": [
      {
        "name": "USDC",
        "address": "0x833589fcd6edb6e08f4c7c32d4f71b54bda02913"
      }
    ]
  }
}
```

### Устранение неполадок

`request denied - max address sponsorship value reached 0x4D44afB8D98c9984F381E60Ac855D89C73f40e45 and sender can not pay with an accepted ERC20”`

Первая часть ошибки указывает на причину, по которой обычная полностью спонсируемая транзакция не была принята. Это ожидаемо. Однако, если "sender can not pay with an accepted ERC20", проверьте, что у пользователя достаточно токенов ERC20 в кошельке для оплаты газа и что ваш кошелек одобрил наш Paymaster для траты этих токенов.
