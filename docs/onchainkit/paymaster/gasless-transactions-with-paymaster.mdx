---
sidebarTitle: 'Бесплатные транзакции на Base с использованием Paymaster'
title: Gasless Бесплатные транзакции на Base с использованием Base Paymaster
description: Узнайте, как использовать Base Paymaster в Coinbase Cloud Developer Platform для проведения транзакций без оплаты газа пользователями.
---

import { Danger } from "/snippets/danger.mdx";

Все еще пытаетесь привлечь пользователей в свое приложение? Хотите избавиться от проблем с оплатой газа и спонсировать его для пользователей на Base? Ответ найден!

Комиссии за транзакции на Base обычно составляют меньше цента, но сама концепция газа может сбивать с толку новых пользователей. Сделайте этот процесс невидимым и улучшите пользовательский опыт с помощью Base Paymaster. Base Paymaster позволяет вам пакетировать многошаговые транзакции и создавать пользовательский опыт без оплаты газа. Спонсируйте до $10 000 в месяц в мейннете (неограниченно в тестнете). Чтобы запросить увеличение лимита, обратитесь в [Discord].

## Цели

- Настроить меры безопасности для обеспечения безопасных и надежных транзакций.
- Управлять и распределять ресурсы для спонсируемых транзакций.
- Субсидировать комиссии за транзакции для пользователей, улучшая их опыт за счет бесплатных транзакций.
- Настраивать и управлять спонсируемыми транзакциями с различной периодичностью: еженедельной, ежемесячной и ежедневной.

## Предварительные требования

Данное руководство предполагает, что у вас есть аккаунт **Coinbase Cloud Developer Platform**. Если нет, зарегистрируйтесь на [CDP site].

### Аккаунт Coinbase CDP 

Это ваша точка доступа к Coinbase Cloud Developer Platform, где вы можете управлять проектами и использовать инструменты, такие как Paymaster.

### Знакомство со смарт-аккаунтами и ERC 4337

Понимание смарт-аккаунтов и стандарта ERC 4337 крайне важно, поскольку они являются основой для выполнения расширенных шаблонов транзакций и абстракции аккаунтов в сети Ethereum.

### Foundry

Foundry - это среда разработки, фреймворк для тестирования и набор инструментов для смарт-контрактов Ethereum. Он необходим для развертывания и тестирования смарт-контрактов.

## Настройка Base Paymaster & Bundler

В этом разделе вы настроите Paymaster для спонсирования платежей от имени определенного смарт-контракта на определенную сумму. Сначала перейдите в Coinbase Developer Platform, создайте или выберите проект и нажмите на инструмент Paymaster в левой панели навигации. Затем перейдите на вкладку Configuration и скопируйте RPC URL в буфер обмена. Он понадобится на следующих шагах в вашем файле index.js.

Перейдите на [Coinbase Developer Platform]:

Создайте или выберите нужный проект в верхнем левом углу экрана.

<Frame>
![cdp-home.png](/images/gasless-transaction-on-base/cdp-select-project.png)
</Frame>

Нажмите на инструмент `Paymaster` в левой панели навигации:\*\* [Paymaster Tool]

<Frame>
![cdp-paymaster-tool.png](/images/gasless-transaction-on-base/cdp-paymaster.png)
</Frame>

Нажмите `Configuration` в верхней части экрана.

<Frame>
![cdp-paymaster-tool.png](/images/gasless-transaction-on-base/cdp-config.png)
</Frame>

Скопируйте RPC URL вашего paymaster в буфер обмена. Он понадобится в вашем файле `index.js` на следующем шаге.

### Добавление контракта в разрешенный список (Allowlist) для спонсирования

Спонсирование транзакций полезно для упрощения онбординга и улучшения UX децентрализованного приложения. Как разработчик, вы хотите обеспечить максимальную безопасность и экономическую эффективность этого процесса. Начните с добавления в разрешенный список контракта по вашему выбору.

Выберите **Base Mainnet**

На странице конфигурации выберите `Base Mainnet` из выпадающего меню. Затем активируйте свой paymaster, нажав на переключатель справа на экране.

<Frame>
![cdp-project-selection.png](/images/gasless-transaction-on-base/cdp-select-project.png)
</Frame>

Добавьте NFT-контракт и функцию mintTo в разрешенный список:

Нажмите кнопку `Add`, чтобы добавить контракт.

Добавьте следующий контракт: [`0x83bd615eb93eE1336acA53e185b03B54fF4A17e8`](https://basescan.org/token/0x83bd615eb93ee1336aca53e185b03b54ff4a17e8)

Укажите `mintTo(address)` как функцию для добавления в разрешенный список, затем нажмите `Save` в нижней части страницы.

<Frame>
![cdp-allowlist-contracts.png](/images/gasless-transaction-on-base/cdp-allowlist-contract.png)
</Frame>

<Info>
**Используйте свой контракт**

Для нашего примера вы будете использовать этот [simple NFT contract], развернутый в основной сети Base. Вы можете использовать любой контракт по своему выбору.
</Info>


### Глобальные и пользовательские лимиты:

Прокрутите вниз до раздела “Per User Limit”

Вы можете установить политику Paymaster, которая определяет либо конкретную сумму в долларах, либо количество UserOperations. Вы можете включить «цикл лимита», который позволяет этой политике обновляться каждую неделю. Эта функция позволяет приложениям спонсировать смарт-кошельки на еженедельной, ежедневной или ежемесячной основе, либо по сумме, либо по количеству UserOperations.

Установите максимум в долларах на $0.05 и максимум UserOperations на 1, чтобы создать политику с максимальным лимитом спонсирования в $0.05 и максимум 1 UserOperation на пользователя.

<Info>
**Циклы лимитов**

Функция Limit Cycles позволяет приложениям спонсировать смарт-кошельки на еженедельной, ежедневной или ежемесячной основе, либо по сумме, либо по количеству UserOperations. Эти лимиты сбрасываются в соответствии с выбранной периодичностью в выпадающем меню.
</Info>


Установите глобальный лимит:

Эта настройка позволяет вам определить максимальную сумму газа или USD, которая может быть спонсирована глобально для всех пользователей. Это помогает контролировать общие расходы на спонсирование газа, гарантируя, что выделенный бюджет не будет превышен. Например, установка глобального лимита в $1 означает, что Paymaster будет спонсировать транзакции, пока общая стоимость газа не достигнет $1.

Установите свою глобальную политику на `$.07`, введя сумму в текстовое поле и нажав кнопку `Save`.

<Frame>
![cdp-allowlist-contracts.png](/images/gasless-transaction-on-base/cdp-global-user-limits.png)
</Frame>

## Тестирование вашей политики Paymaster

Теперь вы протестируете созданную политику.

<Note>
**Использование Foundry**

В этом руководстве вы используете Foundry для создания двух пар ключей, которые позволят создать смарт-аккаунты, от имени которых вы будете спонсировать транзакции. Вам не нужно отправлять какие-либо средства (ETH) на эти кошельки. В качестве альтернативы вы можете использовать приватные ключи уже существующих у вас кошельков.

Foundry зависит от Rust. Если у вас не установлен Rust:

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

Для получения дополнительной информации см. [Foundry Book installation guide](https://book.getfoundry.sh/getting-started/installation).
</Note>


Откройте терминал и установите Foundry, выполнив команду:

```bash
curl -L https://foundry.paradigm.xyz | bash
```

Установите установщик инструментов Foundry:

```bash
foundryup
```

Создайте директорию с именем `sponsored_transactions`:

```bash
mkdir sponsored_transactions
```

Перейдите в новую директорию, инициализируйте проект node, установите две зависимости `viem` и `permissionless` и создайте файл с названием `index.js`:

```bash
cd sponsored_transactions
npm init es6
npm install permissionless
npm install viem
touch index.js
```

В файле `index.js` импортируйте зависимости из viem и permissionless.
Эти зависимости позволят подключиться к Base, создать наши смарт-аккаунты, инициализировать paymaster и отправить наши закодированные данные в сеть.

```javascript
import { http, createPublicClient, encodeFunctionData } from 'viem';
import { base } from 'viem/chains';
import { createSmartAccountClient } from 'permissionless';
import { privateKeyToSimpleSmartAccount } from 'permissionless/accounts';
import { createPimlicoPaymasterClient } from 'permissionless/clients/pimlico';
```

### Установка констант для эндпоинта Paymaster & Bundler

<Info>
**Найдите ваш эндпоинт Paymaster & Bundler**

Эндпоинт Paymaster & Bundler - это URL для вашего Paymaster в Coinbase Developer Platform (CDP).

Он был сохранен в предыдущем разделе и имеет следующий формат: `https://api.developer.coinbase.com/rpc/v1/base/<SPECIAL-KEY>`

Перейдите в [Paymaster Tool] и выберите вкладку `Configuration` в верхней части экрана, чтобы получить ваш RPC URL.
</Info>


<Danger>
**Обезопасьте свои эндпоинты**

Вы создадите константу для нашего эндпоинта Paymaster & Bundler, полученного с cdp.portal.coinbase.com. Наиболее безопасный способ сделать это - использовать прокси. Для целей этой демонстрации мы жестко закодируем его в наш файл `index.js`. Для продакшена мы настоятельно рекомендуем использовать [proxy service].
</Danger>


Вам также понадобится адрес контракта entrypoint для Base. Полный список контрактов entrypoint и их адресов можно найти [здесь](https://docs.alchemy.com/reference/factory-addresses).

Добавьте следующие строки кода после ваших импортов:

```javascript
const rpcUrl = https://api.developer.coinbase.com/rpc/v1/base/<SPECIAL-KEY> //Paymaster & Bundler endpoint
```

```javascript
const baseEntryPoint = '0x5FF137D4b0FDCD49DcA30c7CF57E578a026d2789';
```

```javascript
const baseFactoryAddress = '0x15Ba39375ee2Ab563E8873C8390be6f2E2F50232';
```

Теперь создайте публичный клиент. Публичные клиенты позволяют нашему приложению взаимодействовать с блокчейном Ethereum. Они используют методы JSON-RPC API для выполнения таких действий, как получение номеров блоков, транзакций и чтение данных из смарт-контрактов. Узнайте больше об этом на viem.

Инициализируйте публичный клиент с именем `publicClient` и установите цепочку на `base`, а транспорт на нашу переменную `rpcURL`:

```javascript
const publicClient = createPublicClient({
  chain: base,
  transport: http(rpcUrl),
});
```

### Создание двух смарт-аккаунтов

Теперь вы создадите два `SimpleAccounts` для этой демонстрации. Вместе эти два аккаунта помогут нам протестировать политики безопасности, установленные как на глобальном уровне, так и на уровне пользовательских операций (UserOperations) для каждого аккаунта.

Для этого вам сначала понадобятся два приватных ключа, которые будут использоваться для создания смарт-аккаунтов с помощью Foundry. Откройте терминал и выполните следующую команду **дважды**:

```bash
cast wallet new
```

Вы увидите что-то вроде этого:

```bash
> cast wallet new
Successfully created new keypair.
Address: 0xD440D74620542...D6F005cfD9
Private key: 0x01c9720c1dfa3c9...634793138897

> cast wallet new
Successfully created new keypair.
Address: 0x5f8e5bC8620542...D6F005cfD9
Private key: 0xbcd6fbc1dfa3c9...634793138897
```

Используйте только что сгенерированные приватные ключи в качестве параметра для функции `privateKeyToSimpleSmartAccount` и установите адрес фабрики.

<Danger>
**Обезопасьте свои приватные ключи**

Обязательно сохраните свои приватные ключи в надежном месте. Их размещение в публичном репозитории кода предоставит любому доступ к вашим смарт-аккаунтам.

Самый безопасный способ  сохранить их в файле `.env`.
</Danger>


```javascript
const simpleAccount = await privateKeyToSimpleSmartAccount(publicClient, {
  privateKey: '<Your first key>',
  factoryAddress: baseFactoryAddress, //Base v0.6
  entryPoint: baseEntryPoint,
});

const simpleAccount2 = await privateKeyToSimpleSmartAccount(publicClient, {
  privateKey: '<Your second key>',
  factoryAddress: baseFactoryAddress, //Base v0.6
  entryPoint: baseEntryPoint,
});
```

Давайте разберем, что здесь происходит:
Приватный ключ: приватный ключ кошелька, который вы создали с помощью Foundry или кошелька, который у вас уже есть. Оба варианта подходят.

Адрес фабрики - это адрес фабрики смарт-аккаунтов, развернутой на Base. Фабрики аккаунтов - это смарт-контракты, которые облегчают создание новых контрактов кошельков. Вы можете просмотреть более полный [list of factory addresses] на Alchemy.

Entrypoint - это контракт точки входа для Base.

<Tip>
**Найдите правильный адрес entrypoint**

Выполните JSON RPC запрос к ноде, используя метод `eth_supportedEntryPoints` чтобы получить правильный контракт точки входа. Вот пример для Base с использованием ноды Coinbase Base:

```
curl --request POST \
     --url https://api.developer.coinbase.com/rpc/v1/base/<Your-Key> \
     --header 'Accept: application/json' \
     --header 'Content-Type: application/json' \
     --data '{"id": 1, "jsonrpc": "2.0", "method": "eth_supportedEntryPoints", "params": []}'
```
</Tip>


### Инициализация Paymaster и создание аккаунтов

Инициализируйте paymaster и клиент смарт-аккаунта для обоих смарт-аккаунтов:

```javascript
const cloudPaymaster = createPimlicoPaymasterClient({
  chain: base,
  transport: http(rpcUrl),
  entryPoint: baseEntryPoint,
});

const smartAccountClient = createSmartAccountClient({
  account: simpleAccount,
  chain: base,
  bundlerTransport: http(rpcUrl),
  middleware: {
    sponsorUserOperation: cloudPaymaster.sponsorUserOperation,
  },
});

const smartAccountClient2 = createSmartAccountClient({
  account: simpleAccount2,
  chain: base,
  bundlerTransport: http(rpcUrl),
  middleware: {
    sponsorUserOperation: cloudPaymaster.sponsorUserOperation,
  },
});
```

Для спонсирования транзакций вам понадобится ABI и адрес контрактов, которые вы хотите поддержать.

<Info>
**Спонсируйте любой контракт**

Не стесняйтесь использовать свой собственный контракт для взаимодействия с Paymaster. В учебных целях вы будете взаимодействовать с контрактом ERC-721, развернутым в основной сети Base. Вы вызовете функцию mintTo, которая принимает адрес в качестве единственного параметра.
</Info>


Вы будете взаимодействовать с NFT + ABI из простого NFT-контракта, развернутого по адресу: `0x83bd615eb93eE1336acA53e185b03B54fF4A17e8`

Скопируйте и вставьте ABI NFT в `index.js`

```javascript
const abi = [
  {
    type: 'constructor',
    inputs: [
      { name: '_name', type: 'string', internalType: 'string' },
      { name: '_symbol', type: 'string', internalType: 'string' },
    ],
    stateMutability: 'nonpayable',
  },
  {
    type: 'function',
    name: 'approve',
    inputs: [
      { name: 'spender', type: 'address', internalType: 'address' },
      { name: 'id', type: 'uint256', internalType: 'uint256' },
    ],
    outputs: [],
    stateMutability: 'nonpayable',
  },
  {
    type: 'function',
    name: 'balanceOf',
    inputs: [{ name: 'owner', type: 'address', internalType: 'address' }],
    outputs: [{ name: '', type: 'uint256', internalType: 'uint256' }],
    stateMutability: 'view',
  },
  {
    type: 'function',
    name: 'currentTokenId',
    inputs: [],
    outputs: [{ name: '', type: 'uint256', internalType: 'uint256' }],
    stateMutability: 'view',
  },
  {
    type: 'function',
    name: 'getApproved',
    inputs: [{ name: '', type: 'uint256', internalType: 'uint256' }],
    outputs: [{ name: '', type: 'address', internalType: 'address' }],
    stateMutability: 'view',
  },
  {
    type: 'function',
    name: 'isApprovedForAll',
    inputs: [
      { name: '', type: 'address', internalType: 'address' },
      { name: '', type: 'address', internalType: 'address' },
    ],
    outputs: [{ name: '', type: 'bool', internalType: 'bool' }],
    stateMutability: 'view',
  },
  {
    type: 'function',
    name: 'mintTo',
    inputs: [{ name: 'recipient', type: 'address', internalType: 'address' }],
    outputs: [{ name: '', type: 'uint256', internalType: 'uint256' }],
    stateMutability: 'payable',
  },
  {
    type: 'function',
    name: 'name',
    inputs: [],
    outputs: [{ name: '', type: 'string', internalType: 'string' }],
    stateMutability: 'view',
  },
  {
    type: 'function',
    name: 'ownerOf',
    inputs: [{ name: 'id', type: 'uint256', internalType: 'uint256' }],
    outputs: [{ name: 'owner', type: 'address', internalType: 'address' }],
    stateMutability: 'view',
  },
  {
    type: 'function',
    name: 'safeTransferFrom',
    inputs: [
      { name: 'from', type: 'address', internalType: 'address' },
      { name: 'to', type: 'address', internalType: 'address' },
      { name: 'id', type: 'uint256', internalType: 'uint256' },
    ],
    outputs: [],
    stateMutability: 'nonpayable',
  },
  {
    type: 'function',
    name: 'safeTransferFrom',
    inputs: [
      { name: 'from', type: 'address', internalType: 'address' },
      { name: 'to', type: 'address', internalType: 'address' },
      { name: 'id', type: 'uint256', internalType: 'uint256' },
      { name: 'data', type: 'bytes', internalType: 'bytes' },
    ],
    outputs: [],
    stateMutability: 'nonpayable',
  },
  {
    type: 'function',
    name: 'setApprovalForAll',
    inputs: [
      { name: 'operator', type: 'address', internalType: 'address' },
      { name: 'approved', type: 'bool', internalType: 'bool' },
    ],
    outputs: [],
    stateMutability: 'nonpayable',
  },
  {
    type: 'function',
    name: 'supportsInterface',
    inputs: [{ name: 'interfaceId', type: 'bytes4', internalType: 'bytes4' }],
    outputs: [{ name: '', type: 'bool', internalType: 'bool' }],
    stateMutability: 'view',
  },
  {
    type: 'function',
    name: 'symbol',
    inputs: [],
    outputs: [{ name: '', type: 'string', internalType: 'string' }],
    stateMutability: 'view',
  },
  {
    type: 'function',
    name: 'tokenURI',
    inputs: [{ name: 'id', type: 'uint256', internalType: 'uint256' }],
    outputs: [{ name: '', type: 'string', internalType: 'string' }],
    stateMutability: 'view',
  },
  {
    type: 'function',
    name: 'transferFrom',
    inputs: [
      { name: 'from', type: 'address', internalType: 'address' },
      { name: 'to', type: 'address', internalType: 'address' },
      { name: 'id', type: 'uint256', internalType: 'uint256' },
    ],
    outputs: [],
    stateMutability: 'nonpayable',
  },
  {
    type: 'event',
    name: 'Approval',
    inputs: [
      {
        name: 'owner',
        type: 'address',
        indexed: true,
        internalType: 'address',
      },
      {
        name: 'spender',
        type: 'address',
        indexed: true,
        internalType: 'address',
      },
      { name: 'id', type: 'uint256', indexed: true, internalType: 'uint256' },
    ],
    anonymous: false,
  },
  {
    type: 'event',
    name: 'ApprovalForAll',
    inputs: [
      {
        name: 'owner',
        type: 'address',
        indexed: true,
        internalType: 'address',
      },
      {
        name: 'operator',
        type: 'address',
        indexed: true,
        internalType: 'address',
      },
      { name: 'approved', type: 'bool', indexed: false, internalType: 'bool' },
    ],
    anonymous: false,
  },
  {
    type: 'event',
    name: 'Transfer',
    inputs: [
      { name: 'from', type: 'address', indexed: true, internalType: 'address' },
      { name: 'to', type: 'address', indexed: true, internalType: 'address' },
      { name: 'id', type: 'uint256', indexed: true, internalType: 'uint256' },
    ],
    anonymous: false,
  },
];
```

### Кодирование вызова функции

Закодируйте вызов функции `mintTo` с параметром, который является адресом первого смарт-кошелька.

```javascript
const callData = encodeFunctionData({
  abi: abi,
  functionName: 'mintTo',
  args: [smartAccountClient.account.address],
});
```

Создайте функцию под названием `sendTransactionFromAccount1` для отправки запроса на транзакцию и сохранения ответа в переменной `txHash`:

```javascript
async function sendTransactionFromAccount1() {
  try {
    const txHash = await smartAccountClient.sendTransaction({
      account: smartAccountClient.account,
      to: '0x83bd615eb93eE1336acA53e185b03B54fF4A17e8',
      data: callData,
      value: 0n,
    });

    console.log('✅ Transaction successfully sponsored for account 1!');
    console.log(`🔍 View on Etherscan: https://basescan.org/tx/${txHash}`);
  } catch (error) {
    console.log('Transaction failed from account 1: ', error);
  }
}
```

Вызовите функцию внизу файла `index.js`:

```javascript
async function sendTransactionFromAccount1() {
  // предыдущие строки кода ...
}

sendTransactionFromAccount1();
```

Запустите следующую строку кода из вашего терминала, убедившись, что находитесь в корневой папке:

```bash
node index.js
```

Первая попытка должна быть успешной со следующим выводом:

```bash
✅ Transaction successfully sponsored!
🔍 View on Etherscan: https://basescan.org/tx/0x1439431b0bf333f8106bc1815a532fdec18d62c3ee1c07a10d07711c6e5a2e56
```

Запустите скрипт снова, но на этот раз вы должны получить ошибку, содержащую `code` и `message` например:

```json
{
  "code": -32001,
  "message": "request denied - rejected due to maximum per address transaction count reached"
}
```

Отлично! Ошибки в данном случае — это хорошо. Это означает, что наша политика Paymaster работает и предотвращает нежелательное спонсирование.

Вернитесь на портал CDP и обновите количество UserOperations до `2`.

Подождите около 10-15 минут, чтобы изменения вступили в силу, затем снова запустите `node index.js`, чтобы отправить новую транзакцию.

Должен появиться успешный запрос:

```bash
✅ Transaction successfully sponsored for account 1!
🔍 View on Etherscan: https://basescan.org/tx/0x1439431b0bf333f8106bc1815a532fdec18d62c3ee1c07a10d07711c6e5a2e56
```

Вернувшись к конфигурации нашего Paymaster, вы увидите, что использовали около $0.03 из нашего глобального лимита в $0.05, а для аккаунта 1 достигли лимита UserOperations.

Теперь давайте отправим еще одну транзакцию, на этот раз используя второй аккаунт.

Прежде чем сделать это, давайте обновим лимит UserOperations на кошелек до 10. Поскольку газ на Base дешевый, вам, возможно, придется сделать несколько транзакций, прежде чем будет достигнут наш глобальный лимит в $0.07.

<Info>
**Увеличьте глобальный лимит**

Вы можете получить предупреждение о необходимости увеличения глобального лимита, и это нормально на данный момент...

[Image-of-policy]
</Info>


Вернитесь в и обновите политику до 10, затем нажмите кнопку `Save`.

[image of updated policy]

### Тестирование глобальных лимитов

Вернитесь в редактор кода, откройте файл `index.js` и создайте функцию `sendTransactionFromAccount2`, а затем поместите ее внизу файла `index.js`:

```javascript
// предыдущие строки кода ...

async function sendTransactionFromAccount2() {
  try {
    const txHash2 = await smartAccountClient2.sendTransaction({
      account: smartAccountClient2.account,
      to: '0x83bd615eb93eE1336acA53e185b03B54fF4A17e8',
      data: callData,
      value: 0n,
    });

    console.log('✅ Transaction successfully sponsored for account 2!');
    console.log(`🔍 View on Etherscan: https://basescan.org/tx/${txHash2}`);
  } catch (error) {
    console.log('Transaction failed from account 2: ', error);
  }
}

// sendTransactionFromAccount1();
sendTransactionFromAccount2();
```

Запустите функцию `sendTransactionFromAccount2()` несколько раз, чтобы помочь достичь глобального лимита.

В конце концов вы должны столкнуться с ошибкой, похожей на эту:

```json
{
  "code": -32001,
  "message": "request denied - rejected due to max global usd Spend Permission reached"
}
```

Поздравляем! Достигнут глобальный лимит.

Вернитесь в облачный портал и увеличьте глобальную политику по вашему выбору. Снова запустите `node index.js`, и вы сможете успешно спонсировать транзакцию.

## Заключение

В этом руководстве вы научились настраивать и конфигурировать Base Paymaster на платформе Coinbase Developer Platform, что позволяет вам спонсировать транзакции без газа для ваших пользователей. Это улучшает пользовательский опыт, абстрагируя комиссии за газ и делая транзакции более доступными и экономически эффективными.

---

[list of factory addresses]: https://docs.alchemy.com/reference/factory-addresses
[Discord]: https://discord.com/invite/cdp
[CDP site]: https://portal.cdp.coinbase.com/
[Coinbase Developer Platform]: https://portal.cdp.coinbase.com/
[UI]: https://portal.cdp.coinbase.com/products/bundler-and-paymaster
[proxy service]: https://www.smartwallet.dev/guides/paymasters
[Paymaster Tool]: https://portal.cdp.coinbase.com/products/bundler-and-paymaster
[Foundry Book installation guide]: https://book.getfoundry.sh/getting-started/installation
[simple NFT contract]: https://basescan.org/token/0x83bd615eb93ee1336aca53e185b03b54ff4a17e8

