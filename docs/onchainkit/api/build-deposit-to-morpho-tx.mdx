---
title: buildDepositToMorphoTx
---


Функция `buildDepositToMorphoTx` используется для создания [Calls](/onchainkit/transaction/types#calls) для депозита актива в Morpho. Эти вызовы можно передать в компонент `<Transaction />` для отправки транзакции.

## Использование

```tsx code
import { buildDepositToMorphoTx } from '@coinbase/onchainkit/earn';

const calls = await buildDepositToMorphoTx({
  vaultAddress: '0x...', // Адрес vault Morpho в Base
  tokenAddress: '0x...', // Адрес токена для депозита
  amount: 1000000000000000000n, // Количество токенов для депозита
  recipientAddress: '0x...', // Адрес получателя
});
```

## Возвращает

[`Call[]`](/onchainkit/transaction/types#calls)

## Параметры

[`DepositToMorphoParams`](/onchainkit/earn/types#deposittomorphoparams)

