---
title: TokenSearch
sidebarTitle: TokenSearch
description: Компонент поиска с задержкой дебаунса (устарел)
---

`<TokenSearch />` - это компонент поиска с опциональной задержкой дебаунса.

Если вы хотите обрабатывать задержку дебаунса вне этого компонента, установите `delayMs` в `0`.

## Использование

Используйте [`getTokens`](/onchainkit/api/get-tokens) и `<TokenSearch />` вместе для поиска [`Token`](/onchainkit/token/types#token).

```tsx
import { useCallback } from 'react';
import { base } from 'viem/chains';
// ---cut-before---
import { OnchainKitProvider } from '@coinbase/onchainkit';
import { getTokens } from '@coinbase/onchainkit/api'; // [!code focus]
import { TokenSearch } from '@coinbase/onchainkit/token'; // [!code focus]
import type { Token } from '@coinbase/onchainkit/token'; // [!code focus]

...

// пример асинхронного обработчика onChange
const handleChange = useCallback((value: string) => {
  async function getData(value) {
    const tokens: Token[] = await getTokens({ search: value }); // [!code focus]
    // Сделайте что-то с токенами здесь
  }
  getData(value);
}, []);

...

<OnchainKitProvider
  chain={base}
  apiKey="YOUR_API_KEY"
>
  <TokenSearch onChange={handleChange} delayMs={200} />
</OnchainKitProvider>
```

<iframe
  src="https://684b5e62b1ff46bc5bf83966-aijszlfakk.chromatic.com/iframe.html?args=&id=onchainkit-token-search--default&viewMode=story&dark=true&hero=true"
  width="100%"
  height="auto"
/>

## Свойства

```ts
type TokenSearchProps = {
  className?: string;
  delayMs?: number; // Задержка дебаунса в миллисекундах
  onChange: (value: string) => void; // Функция обратного вызова поиска
};
```
