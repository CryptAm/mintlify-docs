---
title: SwapSettings
sidebarTitle: SwapSettings
description: Настраиваемые компоненты для настроек обмена токенов и конфигурации проскальзывания
---

Компонент `SwapSettings` позволяет настраивать конфигурацию проскальзывания в компоненте `Swap`.

## Использование

```tsx
// опущено для краткости
import {
  Swap,
  SwapSettings,
  SwapSettingsSlippageDescription,
  SwapSettingsSlippageInput,
  SwapSettingsSlippageTitle,
} from '@coinbase/onchainkit/swap';

<Swap>
  <SwapSettings>
    <SwapSettingsSlippageTitle>
      Max. slippage
    </SwapSettingsSlippageTitle>
    <SwapSettingsSlippageDescription>
      Your swap will revert if the prices change by more than the selected
      percentage.
    </SwapSettingsSlippageDescription>
    <SwapSettingsSlippageInput />
  </SwapSettings>
</Swap>
```

<iframe
  src="https://684b5e62b1ff46bc5bf83966-aijszlfakk.chromatic.com/iframe.html?args=&id=onchainkit-swap-settings--default&viewMode=story&dark=true&hero=true"
  width="100%"
  height="520px"
/>

### Переопределение стилей

Вы можете переопределить стили компонента, используя `className`.

```tsx
import {
  Swap,
  SwapSettings,
  SwapSettingsSlippageDescription,
  SwapSettingsSlippageInput,
  SwapSettingsSlippageTitle,
} from '@coinbase/onchainkit/swap';
// ---cut-before---
// опущено для краткости
<Swap>
  <SwapSettings>
    <SwapSettingsSlippageTitle className="text-[#EA580C]">
      Max. slippage
    </SwapSettingsSlippageTitle>
    <SwapSettingsSlippageDescription className="text-[#EA580C]">
      Your swap will revert if the prices change by more than the selected
      percentage.
    </SwapSettingsSlippageDescription>
    <SwapSettingsSlippageInput/>
  </SwapSettings>
</Swap>
```

<iframe
  src="https://684b5e62b1ff46bc5bf83966-aijszlfakk.chromatic.com/iframe.html?args=&id=onchainkit-swap-settings--styled-title-and-description&viewMode=story&dark=true&hero=true"
  width="100%"
  height="520px"
/>

### Переопределение иконки

Вы можете переопределить иконку, используя проп icon.

```tsx
// подавление twoslash ошибки
declare const baseIcon: any;

import {
  Swap,
  SwapSettings,
  SwapSettingsSlippageDescription,
  SwapSettingsSlippageInput,
  SwapSettingsSlippageTitle,
} from '@coinbase/onchainkit/swap';
// ---cut-before---
// опущено для краткости
<SwapSettings icon={baseIcon}>
// ---cut-after---
</SwapSettings>
```

<iframe
  src="https://684b5e62b1ff46bc5bf83966-aijszlfakk.chromatic.com/iframe.html?args=&id=onchainkit-swap-settings--with-icon&viewMode=story&dark=true&hero=true"
  width="100%"
  height="520px"
/>

### Добавление текста

Вы можете добавить текст рядом с компонентом `SwapSettings` используя проп text.

```tsx
import {
  Swap,
  SwapSettings,
  SwapSettingsSlippageDescription,
  SwapSettingsSlippageInput,
  SwapSettingsSlippageTitle,
} from '@coinbase/onchainkit/swap';
// ---cut-before---
// опущено для краткости
<SwapSettings text="Settings">
// ---cut-after---
</SwapSettings>
```

<iframe
  src="https://684b5e62b1ff46bc5bf83966-aijszlfakk.chromatic.com/iframe.html?args=&id=onchainkit-swap-settings--with-text-prop&viewMode=story&dark=true&hero=true"
  width="100%"
  height="520px"
/>

### Переопределение текста

Вы можете переопределить текст компонента, предоставив дочерний текст.

```tsx
import {
  Swap,
  SwapSettings,
  SwapSettingsSlippageDescription,
  SwapSettingsSlippageInput,
  SwapSettingsSlippageTitle,
} from '@coinbase/onchainkit/swap';
<Swap>
// ---cut-before---
// omitted for brevity

<SwapSettingsSlippageTitle>
  Slippage Settings
</SwapSettingsSlippageTitle>
<SwapSettingsSlippageDescription>
  Set a max slippage you are willing to accept.
</SwapSettingsSlippageDescription>
// ---cut-after---
</Swap>
```

<iframe
  src="https://684b5e62b1ff46bc5bf83966-aijszlfakk.chromatic.com/iframe.html?args=&id=onchainkit-swap-settings--custom-title-and-description&viewMode=story&dark=true&hero=true"
  width="100%"
  height="520px"
/>

## Компоненты

- `<SwapSettings />` - Компонент-контейнер для настроек проскальзывания обмена.
- `<SwapSettingsSlippageDescription />` - Отображает описательный текст, объясняющий настройку проскальзывания.
- `<SwapSettingsSlippageInput />` - Поле ввода для пользователей, чтобы ввести желаемый процент максимального проскальзывания
- `<SwapSettingsSlippageTitle />` - Отображает заголовок для раздела настроек проскальзывания

## Свойства

### SwapSettingsProps

```ts
type SwapSettingsProps = {
  children?: ReactNode;
  className?: string; // Опциональное переопределение className для верхнего элемента div
  icon?: ReactNode; // Опциональное переопределение иконки
  text?: string; // Опциональное переопределение текста
};
```

### SwapSettingsSlippageDescriptionProps

```ts
type SwapSettingsSlippageDescriptionProps = {
  children: ReactNode;
  className?: string; // Опциональное переопределение className для верхнего элемента div
};
```

### SwapSettingsSlippageInputProps

```ts
type SwapSettingsSlippageInputProps = {
  className?: string; // Опциональное переопределение className для верхнего элемента div
  render?: (props: {
    slippageSetting: SlippageSettingsType;
    setSlippageSetting: (slippageSetting: SlippageSettingsType) => void;
    setSlippageValue: (number: number) => void;
  }) => ReactNode; // Пользовательская функция рендера для полного контроля над отображением ввода проскальзывания
};
```

### SwapSettingsSlippageTitleProps

```ts
type SwapSettingsSlippageTitleProps = {
  children: ReactNode;
  className?: string; // Опциональное переопределение className для верхнего элемента div
};
```

