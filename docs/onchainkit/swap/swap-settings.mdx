---
title: <SwapSettings /> · OnchainKit
sidebarTitle: <SwapSettings />
description: Настраиваемые компоненты для параметров обмена токенами и конфигурации проскальзывания (slippage)
---

Компонент `SwapSettings` позволяет настраивать конфигурацию проскальзывания (slippage) в компоненте `Swap`.

## Использование

```tsx
// опущено для краткости
import {
  Swap,
  SwapSettings,
  SwapSettingsSlippageDescription,
  SwapSettingsSlippageInput,
  SwapSettingsSlippageTitle,
} from '@coinbase/onchainkit/swap';

<Swap>
  <SwapSettings>
    <SwapSettingsSlippageTitle>
      Max. slippage
    </SwapSettingsSlippageTitle>
    <SwapSettingsSlippageDescription>
      Your swap will revert if the prices change by more than the selected
      percentage.
    </SwapSettingsSlippageDescription>
    <SwapSettingsSlippageInput />
  </SwapSettings>
</Swap>
```

<iframe
  src="https://684b5e62b1ff46bc5bf83966-aijszlfakk.chromatic.com/iframe.html?args=&id=onchainkit-swap-settings--default&viewMode=story&dark=true&hero=true"
  width="100%"
  height="520px"
/>
{/* <App>
  <SwapWrapper>
    {({ address, swappableTokens }) => {
      if (address) {
        return (
          <Swap>
            <SwapSettings>
             <SwapSettingsSlippageTitle>Max. slippage</SwapSettingsSlippageTitle>
             <SwapSettingsSlippageDescription>
              Your swap will revert if the prices change by more than the selected
              percentage.
             </SwapSettingsSlippageDescription>
             <SwapSettingsSlippageInput />
            </SwapSettings>
            <SwapAmountInput
              label="Sell"
              swappableTokens={swappableTokens}
              token={swappableTokens[1]}
              type="from"
            />
            <SwapToggleButton />
            <SwapAmountInput
              label="Buy"
              swappableTokens={swappableTokens}
              token={swappableTokens[2]}
              type="to"
            />
            <SwapButton disabled />
            <SwapMessage />
          </Swap>
        )
      }
      return <>
        <Wallet>
          <ConnectWallet>
            <Avatar className="h-6 w-6" />
            <Name />
          </ConnectWallet>
        </Wallet>
      </>;
    }}
  </SwapWrapper>
</App> */}

### Переопределение стилей

Вы можете переопределить стили компонентов, используя `className`.

```tsx
import {
  Swap,
  SwapSettings,
  SwapSettingsSlippageDescription,
  SwapSettingsSlippageInput,
  SwapSettingsSlippageTitle,
} from '@coinbase/onchainkit/swap';
// ---cut-before---
// опущено для краткости
<Swap>
  <SwapSettings>
    <SwapSettingsSlippageTitle className="text-[#EA580C]">
      Max. slippage
    </SwapSettingsSlippageTitle>
    <SwapSettingsSlippageDescription className="text-[#EA580C]">
      Your swap will revert if the prices change by more than the selected
      percentage.
    </SwapSettingsSlippageDescription>
    <SwapSettingsSlippageInput/>
  </SwapSettings>
</Swap>
```

<iframe
  src="https://684b5e62b1ff46bc5bf83966-aijszlfakk.chromatic.com/iframe.html?args=&id=onchainkit-swap-settings--styled-title-and-description&viewMode=story&dark=true&hero=true"
  width="100%"
  height="520px"
/>
{/* <App>
  <SwapWrapper>
    {({ address, swappableTokens }) => {
      if (address) {
        return (
          <Swap>
            <SwapSettings>
             <SwapSettingsSlippageTitle className="text-[#EA580C]">Max. slippage</SwapSettingsSlippageTitle>
             <SwapSettingsSlippageDescription className="text-[#EA580C]">
              Your swap will revert if the prices change by more than the selected
              percentage.
             </SwapSettingsSlippageDescription>
             <SwapSettingsSlippageInput/>
            </SwapSettings>
            <SwapAmountInput
              label="Sell"
              swappableTokens={swappableTokens}
              token={swappableTokens[1]}
              type="from"
            />
            <SwapToggleButton />
            <SwapAmountInput
              label="Buy"
              swappableTokens={swappableTokens}
              token={swappableTokens[2]}
              type="to"
            />
            <SwapButton disabled />
            <SwapMessage />
          </Swap>
        )
      }
      return <>
        <Wallet>
          <ConnectWallet>
            <Avatar className="h-6 w-6" />
            <Name />
          </ConnectWallet>
        </Wallet>
      </>;
    }}
  </SwapWrapper>
</App> */}

### Переопределение иконки

Вы можете переопределить иконку, используя свойство `icon`.

```tsx
// suppress twoslash error
declare const baseIcon: any;

import {
  Swap,
  SwapSettings,
  SwapSettingsSlippageDescription,
  SwapSettingsSlippageInput,
  SwapSettingsSlippageTitle,
} from '@coinbase/onchainkit/swap';
// ---cut-before---
// опущено для краткости
<SwapSettings icon={baseIcon}>
// ---cut-after---
</SwapSettings>
```

<iframe
  src="https://684b5e62b1ff46bc5bf83966-aijszlfakk.chromatic.com/iframe.html?args=&id=onchainkit-swap-settings--with-icon&viewMode=story&dark=true&hero=true"
  width="100%"
  height="520px"
/>
{/* <App>
  <SwapWrapper>
    {({ address, swappableTokens }) => {
      if (address) {
        return (
          <Swap>
            <SwapSettings icon={walletDropdownLinkCustomBaseIconSvg}>
             <SwapSettingsSlippageTitle>Max. slippage</SwapSettingsSlippageTitle>
             <SwapSettingsSlippageDescription>
              Your swap will revert if the prices change by more than the selected
              percentage.
             </SwapSettingsSlippageDescription>
             <SwapSettingsSlippageInput/>
            </SwapSettings>
            <SwapAmountInput
              label="Sell"
              swappableTokens={swappableTokens}
              token={swappableTokens[1]}
              type="from"
            />
            <SwapToggleButton />
            <SwapAmountInput
              label="Buy"
              swappableTokens={swappableTokens}
              token={swappableTokens[2]}
              type="to"
            />
            <SwapButton disabled />
            <SwapMessage />
          </Swap>
        )
      }
      return <>
        <Wallet>
          <ConnectWallet>
            <Avatar className="h-6 w-6" />
            <Name />
          </ConnectWallet>
        </Wallet>
      </>;
    }}
  </SwapWrapper>
</App> */}

### Добавление текста

Вы можете добавить текст рядом с компонентом `SwapSettings` используя свойство `text`.

```tsx
import {
  Swap,
  SwapSettings,
  SwapSettingsSlippageDescription,
  SwapSettingsSlippageInput,
  SwapSettingsSlippageTitle,
} from '@coinbase/onchainkit/swap';
// ---cut-before---
// опущено для краткости
<SwapSettings text="Settings">
// ---cut-after---
</SwapSettings>
```

<iframe
  src="https://684b5e62b1ff46bc5bf83966-aijszlfakk.chromatic.com/iframe.html?args=&id=onchainkit-swap-settings--with-text-prop&viewMode=story&dark=true&hero=true"
  width="100%"
  height="520px"
/>
{/* <App>
  <SwapWrapper>
    {({ address, swappableTokens }) => {
      if (address) {
        return (
          <Swap>
            <SwapSettings text="Settings">
             <SwapSettingsSlippageTitle>Max. slippage</SwapSettingsSlippageTitle>
             <SwapSettingsSlippageDescription>
              Your swap will revert if the prices change by more than the selected
              percentage.
             </SwapSettingsSlippageDescription>
             <SwapSettingsSlippageInput/>
            </SwapSettings>
            <SwapAmountInput
              label="Sell"
              swappableTokens={swappableTokens}
              token={swappableTokens[1]}
              type="from"
            />
            <SwapToggleButton />
            <SwapAmountInput
              label="Buy"
              swappableTokens={swappableTokens}
              token={swappableTokens[2]}
              type="to"
            />
            <SwapButton disabled />
            <SwapMessage />
          </Swap>
        )
      }
      return <>
        <Wallet>
          <ConnectWallet>
            <Avatar className="h-6 w-6" />
            <Name />
          </ConnectWallet>
        </Wallet>
      </>;
    }}
  </SwapWrapper>
</App> */}

### Переопределение текста

Вы можете переопределить текст компонента, предоставив дочерний текст.

```tsx
import {
  Swap,
  SwapSettings,
  SwapSettingsSlippageDescription,
  SwapSettingsSlippageInput,
  SwapSettingsSlippageTitle,
} from '@coinbase/onchainkit/swap';
<Swap>
// ---cut-before---
// опущено для краткости

<SwapSettingsSlippageTitle>
  Slippage Settings
</SwapSettingsSlippageTitle>
<SwapSettingsSlippageDescription>
  Set a max slippage you are willing to accept.
</SwapSettingsSlippageDescription>
// ---cut-after---
</Swap>
```

<iframe
  src="https://684b5e62b1ff46bc5bf83966-aijszlfakk.chromatic.com/iframe.html?args=&id=onchainkit-swap-settings--custom-title-and-description&viewMode=story&dark=true&hero=true"
  width="100%"
  height="520px"
/>
{/* <App>
  <SwapWrapper>
    {({ address, swappableTokens }) => {
      if (address) {
        return (
          <Swap>
            <SwapSettings>
             <SwapSettingsSlippageTitle>Slippage Settings</SwapSettingsSlippageTitle>
             <SwapSettingsSlippageDescription>
              Set a max slippage you are willing to accept.
             </SwapSettingsSlippageDescription>
             <SwapSettingsSlippageInput/>
            </SwapSettings>
            <SwapAmountInput
              label="Sell"
              swappableTokens={swappableTokens}
              token={swappableTokens[1]}
              type="from"
            />
            <SwapToggleButton />
            <SwapAmountInput
              label="Buy"
              swappableTokens={swappableTokens}
              token={swappableTokens[2]}
              type="to"
            />
            <SwapButton disabled />
            <SwapMessage />
          </Swap>
        )
      }
      return <>
        <Wallet>
          <ConnectWallet>
            <Avatar className="h-6 w-6" />
            <Name />
          </ConnectWallet>
        </Wallet>
      </>;
    }}
  </SwapWrapper>
</App> */}

## Компоненты

- `<SwapSettings />` - Контейнерный компонент для настроек проскальзывания (slippage) при обмене.
- `<SwapSettingsSlippageDescription />` - Отображает пояснительный текст для настройки проскальзывания.
- `<SwapSettingsSlippageInput />` - Поле ввода, в котором пользователь может указать желаемый максимальный процент проскальзывания.
- `<SwapSettingsSlippageTitle />` - Отображает заголовок для раздела настроек проскальзывания.

## Свойства

- [`SwapSettingsReact`](/onchainkit/swap/types#swapsettingsreact)
- [`SwapSettingsSlippageDescriptionReact`](/onchainkit/swap/types#swapsettingsslippagedescriptionreact)
- [`SwapSettingsSlippageInputReact`](/onchainkit/swap/types#swapsettingsslippageinputreact)
- [`SwapSettingsSlippageTitleReact`](/onchainkit/swap/types#swapsettingsslippagetitlereact)

