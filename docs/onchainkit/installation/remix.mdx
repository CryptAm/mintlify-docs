---
title: Установка для Remix · OnchainKit
sidebarTitle: Установка для Remix
description: Установите OnchainKit с использованием Remix
---

import StartBuilding from "/snippets/start-building.mdx";

Установите и настройте OnchainKit вместе с Remix.
Если вы интегрируете OnchainKit в существующий проект,
перейдите сразу к разделу [установки OnchainKit](#install-onchainkit).

<Steps>
  <Step title="Установите Remix">
  Создайте новый проект Remix с помощью Remix CLI. 
  Подробную информацию о Remix можно найти [здесь](https://remix.run/docs/en/main/start/quickstart).

  ```bash Terminal
  npx create-remix@latest
  ```
  </Step>
  <Step title="Установите OnchainKit">
  Добавьте OnchainKit в ваш проект, установив пакет `@coinbase/onchainkit`.

  <CodeGroup>
  ```bash npm
  npm install @coinbase/onchainkit
  ```

  ```bash yarn
  yarn add @coinbase/onchainkit
  ```

  ```bash pnpm
  pnpm add @coinbase/onchainkit
  ```

  ```bash bun
  bun add @coinbase/onchainkit
  ```
  </CodeGroup>
  </Step>
  <Step title="Получите Client API Key">

  Получите ваш [Client API Key](https://portal.cdp.coinbase.com/projects/api-keys/client-key) от Coinbase Developer Platform.

<Frame>
  <img
    alt="OnchainKit copy Client API Key"
    src="/images/onchainkit/copy-api-key-guide.png"
    height="364"
  />
</Frame>

  Создайте файл `.env` в корневой директории вашего проекта.

<Frame>
  <img
    alt="OnchainKit define Client API Key"
    src="/images/onchainkit/getting-started-create-env-file.png"
    width="250"
    loading="lazy"
  />
</Frame>

  Добавьте ваш Client API Key в файл `.env`:

  ```dotenv .env
  PUBLIC_ONCHAINKIT_API_KEY=YOUR_CLIENT_API_KEY
  ```

  Обновите файл `app/root.tsx`, чтобы предоставить доступ к вашему Client API Key
  через `window.ENV`:

  ```tsx app/root.tsx
  declare global {
    interface Window {
      ENV: {
        PUBLIC_ONCHAINKIT_API_KEY: string; // [!code ++]
      };
    }
  }

  export async function loader() {
    return json({
      ENV: {
        PUBLIC_ONCHAINKIT_API_KEY: process.env.PUBLIC_ONCHAINKIT_API_KEY, // [!code ++]
      },
    });
  }
  ```

  Если это первая переменная окружения, которую вы добавляете в проект, вам потребуется
  обновить компонент Layout в app/root.tsx, чтобы сделать её доступной для вашего приложения:

  ```tsx app/root.tsx
  export function Layout({ children }: { children: React.ReactNode }) {
    const data = useLoaderData<typeof loader>(); // [!code ++]
    return (
      <html lang="en">
        <head>
          <meta charSet="utf-8" />
          <meta name="viewport" content="width=device-width, initial-scale=1" />
          <Meta />
          <Links />
        </head>
        <body>
          {children}
          <ScrollRestoration />
          <script // [!code ++]
            dangerouslySetInnerHTML={{ // [!code ++]
              __html: `window.ENV = ${JSON.stringify( // [!code ++]
                data.ENV // [!code ++]
              )}`, // [!code ++]
            }} // [!code ++]
          />
          <Scripts />
        </body>
      </html>
    );
  }
  ```
  </Step>
  <Step title="Добавьте провайдеры">
  Создайте файл `providers.tsx`. Добавьте `OnchainKitProvider` с нужной вам конфигурацией.

  Внутри OnchainKit автоматически создаст наших рекомендованных провайдеров Wagmi и QueryClient. 
  Если вы хотите их кастомизировать, ознакомьтесь с разделом [Пользовательские 
  дополнительные провайдеры](/onchainkit/config/supplemental-providers).

  ```tsx providers.tsx
  // @noErrors: 2307 2580 2339 - cannot find 'process', cannot find './wagmi', cannot find 'import.meta'
  'use client';

  import type { ReactNode } from 'react';
  import { OnchainKitProvider } from '@coinbase/onchainkit';
  import { base } from 'wagmi/chains'; // добавьте baseSepolia для тестирования // [!code ++]

  export function Providers(props: { children: ReactNode }) {
    const apiKey =
      typeof window !== 'undefined'
        ? window.ENV?.PUBLIC_ONCHAINKIT_API_KEY
        : undefined;
    return (
      <OnchainKitProvider
        apiKey={apiKey} // [!code ++]
        chain={base} // добавьте baseSepolia для тестирования // [!code ++]
      >
        {props.children}
      </OnchainKitProvider>
    );
  }
  ```
  </Step>
  <Step title="Оберните ваше приложение в <AppProviders />">
  После настройки провайдеров в шаге 4, оберните ваше приложение 
  компонентом `<AppProviders />`.

  ```tsx app.tsx
  import { AppProviders } from 'src/AppProviders';

  export default function App() {
    return (
      <AppProviders>
        <Outlet />
      </AppProviders>
    );
  }
  ```
  </Step>
  <Step title="Импортируйте стили OnchainKit">
  Компоненты OnchainKit поставляются с предустановленными стилями. 
  Чтобы подключить эти стили в ваш проект, добавьте следующую строку импорта 
  в начало файла, где импортируется `<AppProviders />`:

  ```tsx app.tsx
  import { AppProviders } from 'src/AppProviders';
  import '@coinbase/onchainkit/styles.css'; // [!code ++]

  export default function App() {
    return (
      <AppProviders>
        <Outlet />
      </AppProviders>
    );
  }
  ```

  Это гарантирует, что стили OnchainKit будут загружены и применены ко всему вашему приложению.

  - Пользователям Tailwind CSS рекомендуем ознакомиться с нашим [Руководством по интеграции с Tailwind](/onchainkit/guides/tailwind).

  - Изменяйте внешний вид компонентов с помощью наших встроенных тем или создавайте свою собственную тему.
    Изучите возможности в нашем [Руководстве по темам](/onchainkit/guides/themes).

  </Step>
</Steps>

<StartBuilding />

