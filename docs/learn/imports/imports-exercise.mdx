---
title: Упражнение по импортам
description: Упражнение - продемонстрируйте свои знания по импортам.
sidebarTitle: Упражнение
hide_table_of_contents: false
---


Создайте контракт, соответствующий следующим спецификациям.

---

## Контракт

Создайте контракт с именем `ImportsExercise`. Он должен `import` копию `SillyStringUtils`

```solidity
// SPDX-License-Identifier: MIT

pragma solidity ^0.8.17;

library SillyStringUtils {

    struct Haiku {
        string line1;
        string line2;
        string line3;
    }

    function shruggie(string memory _input) internal pure returns (string memory) {
        return string.concat(_input, unicode" 🤷");
    }
}
```

Добавьте публичный экземпляр `Haiku` с именем `haiku`.

Добавьте следующие две функции.

### Сохранение Haiku

`saveHaiku` должна принимать три строки и сохранять их как строки `haiku`.

### Получение Haiku

`getHaiku` должна возвращать хайку как тип `Haiku`.

<Note>
Помните, что компилятор автоматически создаст геттер для `public` `struct`, но он возвращает каждый член по отдельности. Создайте собственный геттер, чтобы возвращать тип целиком.
</Note>


### Haiku с пожиманием плечами

`shruggieHaiku` должна использовать библиотеку, чтобы добавить 🤷 в конец `line3`. Она **не должна** изменять оригинальное хайку. Функция должна возвращать модифицированный `Haiku`.

---

## Отправьте свой контракт и получите NFT-значок! (BETA)

<Warning>
#### Рекомендации по верификации контрактов

Для упрощения верификации вашего контракта в обозревателе блокчейна, таком как BaseScan.org, рассмотрите две распространенные стратегии:

1. **Выравнивание**: Этот метод предполагает объединение вашего основного контракта и всех его импортированных зависимостей в один файл. Это упрощает верификацию кода для обозревателей, поскольку им нужно обрабатывать только один файл.

2. **Модульное развертывание**: Альтернативно, вы можете развернуть каждый импортированный контракт отдельно, а затем ссылаться на них в основном контракте через их развернутые адреса. Этот подход сохраняет модульность и читаемость вашего кода. Каждый контракт развертывается и верифицируется независимо, что может облегчить обновления и повторное использование.

3. **Используйте настольные инструменты**: Forge и Hardhat имеют инструменты для написания скриптов, которые одновременно развертывают и верифицируют ваши контракты.
</Warning>


<Note>
#### Эй, куда делся мой NFT?!

[Тестовые сети](/learn/deployment-to-testnet/test-networks) не являются постоянными! Base Goerli [скоро будет приостановлена](https://base.mirror.xyz/kkz1-KFdUwl0n23PdyBRtnFewvO48_m-fZNzPMJehM4), в пользу Base Sepolia.

Поскольку это отдельные сети с отдельными данными, ваши NFT **не будут** перенесены.

**Не волнуйтесь!** Мы зафиксировали адреса всех владельцев NFT в Base Goerli и включим их, когда позже в этом году выпустим механизм для переноса этих NFT в основную сеть (mainnet)! Вы также можете повторно развернуть в Sepolia и отправить заново, если хотите!
</Note>


{/* <CafeUnitTest nftNum={19}/> */}

<iframe
  src="https://684b5e62b1ff46bc5bf83966-aijszlfakk.chromatic.com/iframe.html?args=&id=components-cafeunittest--nineteen&viewMode=story&dark=true&hero=true"
  width="100%"
  height="auto"
/>
