---
title: Настройка и обзор
description: Обзор Hardhat, фреймворка для разработки смарт-контрактов
hide_table_of_contents: false
---

# Настройка и обзор

В этой статье вы узнаете о Hardhat: фреймворке для разработки, позволяющем создавать, тестировать и развертывать смарт-контракты в Ethereum и других поддерживаемых цепях EVM.

---

## Цели обучения

После изучения этого урока вы сможете:

- Установить и создать новый проект Hardhat с поддержкой Typescript
- Описать организацию и структуру папок проекта Hardhat
- Перечислить назначение и свойства файла hardhat.config.ts

---

## Обзор

[Hardhat] - это среда разработки, которая позволяет разрабатывать и тестировать Solidity на вашем локальном компьютере. Она включает инструменты отладки и модульного тестирования и имеет экосистему плагинов, разработанных сторонними разработчиками, которые упрощают разработку и развертывание.

Среди прочего, эти плагины могут помочь вам развертывать контракты, видеть размер вашего скомпилированного байт-кода и даже видеть покрытие модульными тестами.

## Установка Hardhat и создание нового проекта

В качестве предварительного требования для начала разработки смарт-контрактов с Hardhat должен быть установлен Node.js.

Затем вы можете просто ввести `npx hardhat init`, что предоставляет набор опций для начальной загрузки проекта Hardhat:

```
888    888                      888 888               888
888    888                      888 888               888
888    888                      888 888               888
8888888888  8888b.  888d888 .d88888 88888b.   8888b.  888888
888    888     "88b 888P"  d88" 888 888 "88b     "88b 888
888    888 .d888888 888    888  888 888  888 .d888888 888
888    888 888  888 888    Y88b 888 888  888 888  888 Y88b.
888    888 "Y888888 888     "Y88888 888  888 "Y888888  "Y888

👷 Добро пожаловать в Hardhat v2.11.2 👷‍

? Что вы хотите сделать? …
❯ Создать JavaScript проект
  Создать TypeScript проект
  Создать пустой hardhat.config.js
  Выйти
```

Вам рекомендуется выбрать **Create a TypeScript project** (Создать TypeScript проект), поскольку это дает вам такие преимущества, как статическая типизация, которая может уменьшить количество ошибок во время разработки.

Затем вы можете ввести 'yes' для оставшихся опций, которые включают установку пакета `@nomicfoundation/hardhat-toolbox`, содержащего некоторые из наиболее используемых плагинов Hardhat.

```
✔ Что вы хотите сделать? · Создать TypeScript проект
✔ Корень проекта Hardhat: · {любое место}
✔ Хотите добавить .gitignore? (Y/n) · y
✔ Хотите установить зависимости этого примерного проекта с помощью npm (hardhat @nomicfoundation/hardhat-toolbox)? (Y/n) · y
```

### Структура проекта Hardhat

После завершения предыдущего шага структура папок выглядит следующим образом:

- contracts # здесь будут находиться контракты
- hardhat.config.ts # файл конфигурации для Hardhat
- node_modules # папка пакетов node.js
- package-lock.json # файл блокировки пакетов node.js
- package.json # файл пакета node.js
- scripts # место для скриптов
- test # место для тестов
- tsconfig.json # файл конфигурации typescript

Также принято сохранять задачи Hardhat в папке `task`.

Важно отметить, что все эти пути полностью настраиваются в файле `hardhat.config.ts`. Вы можете указать другую папку для контрактов, например, `src`.

### Конфигурация

Вы можете настроить среду Hardhat в файле `hardhat.config.ts`.

Поскольку проект использует TypeScript, у вас есть преимущество использования статической типизации.

По умолчанию конфигурация следующая:

```tsx
import { HardhatUserConfig } from 'hardhat/config';
import '@nomicfoundation/hardhat-toolbox';

const config: HardhatUserConfig = {
  solidity: '0.8.17',
};

export default config;
```

Вы можете настроить такие аспекты, как:

- сеть по умолчанию
- сети
- Solidity
- пути
- mocha

Например:

```tsx
import { HardhatUserConfig } from 'hardhat/config';
import '@nomicfoundation/hardhat-toolbox';

const config: HardhatUserConfig = {
  defaultNetwork: 'base',
  networks: {
    base_sepolia: {
      url: 'https://sepolia.base.org',
      accounts: ['<private key 1>'],
    },
    sepolia: {
      url: 'https://sepolia.infura.io/v3/<key>',
      accounts: ['<private key 1>', '<private key 2>'],
    },
  },
  solidity: {
    version: '0.8.17',
    settings: {
      optimizer: {
        enabled: true,
        runs: 200,
      },
    },
  },
  paths: {
    sources: './contracts',
    tests: './test',
    cache: './cache',
    artifacts: './artifacts',
  },
};

export default config;
```

## Компиляция смарт-контрактов

На этом этапе у вас должен быть запущенный проект Hardhat для начала разработки смарт-контрактов. Вы можете заметить, что Hardhat включает пример контракта под названием `Lock.sol`.

Чтобы запустить вашу первую команду, введите `npx hardhat compile`, которая компилирует смарт-контракты и генерирует правильные [artifacts](https://hardhat.org/hardhat-runner/docs/advanced/artifacts) включающие байт-код и ABI.

После выполнения команды `npx hardhat compile` вы должны увидеть новую папку с именем artifacts. Эта папка содержит каждое имя контракта как папку и файл
`{ContractName}.json` file.

---

[Solidity Docs]: https://docs.soliditylang.org/en/v0.8.17/
[Remix Project]: https://remix-project.org/
[Hardhat]: https://hardhat.org/
[Compilation Artifacts]: https://hardhat.org/hardhat-runner/docs/advanced/artifacts
