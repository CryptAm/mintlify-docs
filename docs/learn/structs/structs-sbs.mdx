---
title: Структуры
description: Практикуйтесь в использовании структур.
hide_table_of_contents: false
sidebarTitle: Пошаговое руководство
---

Тип `struct` позволяет организовать связанные данные разных типов.

---

## Цели

К концу этого урока вы должны уметь:

- Создавать `struct` (пользовательский тип), содержащий несколько различных типов данных
- Объявлять члены `struct` для максимизации эффективности хранилища
- Описывать ограничения, связанные с присвоением `struct` в зависимости от содержащихся в них типов

---

## Создание структуры

В последнем упражнении мы использовали `mapping` для создания связи между `address` и `uint`. Но что, если у ваших пользователей тоже есть любимые цвета? Или любимые автомобили? Вы могли бы создать `mapping` для каждого из них, но это быстро станет неудобным. Вместо этого [`struct`](https://docs.soliditylang.org/en/v0.8.17/types.html#structs) можно использовать для создания пользовательского типа, который может хранить все избранное пользователя в одном типе данных.

Создайте новый контракт с именем `Structs`.

### Настройка структуры

Создайте `struct` с помощью ключевого слова, за которым следует имя типа, фигурные скобки и переменные, составляющие тип. Добавьте заглушку для `Favorites`:

```solidity
struct Favorites {

}
```

После консультации с дизайнерами нам нужно хранить следующее для избранного каждого адреса:

- Любимое число
- День рождения (число месяца)
- Любимый цвет
- Счастливые лотерейные номера

Давайте остановимся на мгновение и проведем техническое проектирование того, как сохранить наши избранные данные.

Команда продукта подтвердила нам, что мы можем с уверенностью ожидать, что ни у одного пользователя нет любимого числа больше 65 536, и, конечно же, все родились в день месяца от 1 до 31.

[Упаковка переменных](https://docs.soliditylang.org/en/v0.8.17/internals/layout_in_storage.html) также работает внутри структур, поэтому мы потенциально можем сэкономить на хранилище, используя меньшие `uint` для этих переменных. Однако люди не так часто меняют свое любимое число, а день месяца, в который они родились, никогда не меняется.

Следовательно, вероятно, более эффективным по газу и менее громоздким для написания других частей кода будет просто использовать `uint` для обеих переменных.

Любимый цвет может быть `string`.

Для счастливых лотерейных номеров нам нужна коллекция. Мы могли бы использовать динамический массив, поскольку это будет в _хранилище_, но мы уже знаем, что в лотерее 5 номеров.

Попробуйте использовать эту информацию, чтобы самостоятельно построить `struct`. У вас должно получиться что-то похожее на:

<Accordion title="Показать код">

```solidity
struct Favorites {
    uint favoriteNumber;
    uint birthDay;
    string favoriteColor;
    uint[5] lotteryNumbers;
}
```

</Accordion>

### Создание экземпляра структуры с использованием ее имени

Есть два способа создать экземпляр структуры, используя ее имя. Первый похож на создание нового объекта в JavaScript:

```solidity
Favorites memory myFavorites = Favorites({
    favoriteNumber: 29,
    birthDay: 14,
    favoriteColor: "red",
    lotteryNumbers: [uint(1), 2, 3, 4, 5]
});
```

Вы также можете использовать сокращенный метод, в котором вы пропускаете имена членов и просто перечисляете значение для каждого из них. Обратите внимание, что в этом формате фигурные скобки **не** используются:

```solidity
Favorites memory myFavorites = Favorites(
    29,
    14,
    "red",
    [uint(1), 2, 3, 4, 5]
);
```

Нет разницы в затратах газа между этими методами. Используйте тот, который имеет наибольший смысл в данной ситуации.

### Сохранение нескольких экземпляров в хранилище

Далее нам нужно выяснить лучший способ организовать `Favorites` в _хранилище_. Есть несколько вариантов, как всегда, каждый со своими компромиссами. Вы можете использовать шаблон, который вы использовали для любимых чисел, и использовать `mapping` для сопоставления `addresses` с `Favorites`.

Другой популярный метод - использовать массив, который использует преимущества того, что `.push` возвращает ссылку на только что добавленный элемент, и тот факт, что концепция _undefined_ не существует в Solidity.

Сначала создайте массив `Favorites`:

```solidity
Favorites[] public userFavorites;
```

Затем добавьте `public` функцию для добавления отправленных избранных данных в список. Она должна принимать каждый из членов в качестве аргумента. Затем присвойте каждый аргумент новому элементу через ссылку, возвращенную `push()`.

<Accordion title="Показать код">

```solidity
function addFavorite(
    uint _favoriteNumber,
    uint _birthDay,
    string calldata _favoriteColor,
    uint[5] calldata _lotteryNumbers
) public {
    // .push() возвращает ссылку на новый элемент
    Favorites storage newFavorite = userFavorites.push();
    newFavorite.favoriteNumber = _favoriteNumber;
    newFavorite.birthDay = _birthDay;
    newFavorite.favoriteColor = _favoriteColor;
    newFavorite.lotteryNumbers = _lotteryNumbers;
}
```

</Accordion>

В качестве альтернативы вы можете создать экземпляр в памяти, а затем `push` его в хранилище.

<Accordion title="Показать код">

```solidity
function addFavorite(
    uint _favoriteNumber,
    uint _birthDay,
    string calldata _favoriteColor,
    uint[5] calldata _lotteryNumbers
) public {
    Favorites memory myFavorites = Favorites(
        29,
        14,
        "red",
        [uint(1), 2, 3, 4, 5]
    );

    userFavorites.push(myFavorites);
}
```

</Accordion>

Затраты газа схожи для каждого из этих методов.

---

## Неожиданное поведение в структурах

Структуры в Solidity демонстрируют некоторые неожиданные или даже разочаровывающие свойства. Работа с ними часто включает распутывание набора взаимоисключающих свойств и потребностей.

### Динамические массивы хранилища в структурах

Команда продукта связалась с вами, чтобы сообщить, что бета-тестеры жалуются на `lotteryNumbers`. Оказывается, не в каждой местности есть лотереи, в которых разыгрывается 5 номеров. В некоторых их 3, 4 или даже 6!

Вы можете подумать, что это достаточно простое изменение. В конце концов, вы можете просто удалить размер из объявления массива внутри `Favorites`. Попробуйте сделать это:

```solidity
struct Favorites {
    uint favoriteNumber;
    uint birthDay;
    string favoriteColor;
    uint[] lotteryNumbers; // Убрали '5'
}
```

Вы получите ошибку, если используете метод `memory`, показанный выше.

```text
from solidity:
TypeError: Invalid type for argument in function call. Invalid implicit conversion from uint256[5] memory to uint256[] memory requested.
  --> contracts/mappings_exercise.sol:70:13:
   |
70 |             [uint(1), 2, 3, 4, 5]
   |             ^^^^^^^^^^^^^^^^^^^^^
```

Самое простое решение здесь — вернуться к использованию `push()` для создания пустого экземпляра `Favorites`, а затем присвоить значения.

Причина, по которой это работает, немного непонятна. В неудачном примере ожидаемым типом аргумента является массив `uint` без размера, но предоставлен массив `uint` с размером. Solidity не может выполнять неявные преобразования подобного рода в большинстве случаев, и вы получите ошибку компилятора, если предоставите неправильный тип для аргумента, даже если он преобразуем.

Одним из исключений из этого правила является то, что Solidity **может** выполнять неявное преобразование во время _присваивания_, если переменная в правой части "помещается" в переменную в левой части.

`uint[5]` помещается в `uint[]`, поэтому Solidity позволит ему сидеть 🐈.

Но что произойдет, если вы используете геттер для `userFavorites`, чтобы получить вашу запись?

```text
{
	"0": "uint256: favoriteNumber 29",
	"1": "uint256: birthDay 14",
	"2": "string: favoriteColor red"
}
```

Что случилось с массивом? Его нет, и оказывается, что это [преднамеренно](https://github.com/ethereum/solidity/issues/1626).

### Отображения (Mappings) внутри структур

Вы можете добавить `mapping` внутри `struct`, с некоторыми особенностями и ограничениями. Добавьте `mapping (uint => uint) numberPairs`; в `Favorites`.

В `addFavorites` присвойте `newFavorite.numberPairs[33] = 66`;

Разверните и протестируйте. Пока что все хорошо!

_Дежавю_ впереди: Но что произойдет, если вы используете _геттер_ для `userFavorites`, чтобы получить вашу запись?

```text
{
	"0": "uint256: favoriteNumber 29",
	"1": "uint256: birthDay 14",
	"2": "string: favoriteColor red"
}
```

Его нет, и оказывается, что это [преднамеренно](https://github.com/ethereum/solidity/issues/1626).

Другая проблема возникает, если вы попытаетесь вернуть структуру из публичной функции. Что, если вы хотите, чтобы ваша функция `addFavorite` возвращала ссылку на новое избранное?

```solidity
// Плохой пример кода, не будет работать
function addFavorite(
    uint _favoriteNumber,
    uint _birthDay,
    string calldata _favoriteColor,
    uint[] calldata _lotteryNumbers
) public returns (newFavorite memory) {
    // .push() возвращает ссылку на новый элемент
    Favorites storage newFavorite = userFavorites.push();
    newFavorite.favoriteNumber = _favoriteNumber;
    newFavorite.birthDay = _birthDay;
    newFavorite.favoriteColor = _favoriteColor;
    newFavorite.lotteryNumbers = _lotteryNumbers;
    newFavorite.numberPairs[33] = 66;

    return newFavorite;
}
```

Вы получите ошибку. Тип `mapping` не может быть возвращен `public` или `external` функцией, поэтому и `struct`, содержащий его, тоже не может.

```text
from solidity:
TypeError: Types containing (nested) mappings can only be parameters or return variables of internal or library functions.
  --> contracts/mappings_exercise.sol:64:23:
   |
64 |     ) public returns (Favorites memory) {
   |                       ^^^^^^^^^^^^^^^^
```

Наконец, что произойдет, если вы попытаетесь присвоить `newFavorite` переменной `memory`? Опять возникает ошибка, потому что `mapping` могут быть только в `storage`.

```solidity
// Плохой пример кода, не будет работать
Favorites memory secondFavorite = newFavorite;
```

```text
from solidity:
TypeError: Type struct Structs.Favorites memory is only valid in storage because it contains a (nested) mapping.
  --> contracts/mappings_exercise.sol:82:9:
   |
82 |         Favorites memory secondFavorite = newFavorite;
   |         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
```

### Автоматические геттеры для публичных структур

Как и с другими типами, если вы поместите `public` `struct` в хранилище на уровне контракта, компилятор автоматически сгенерирует геттер. Однако они работают не совсем так, как вы могли бы ожидать. Например, представьте:

```solidity

struct MyStruct {
    uint first;
    uint second;
    uint third;
}

MyStruct myStruct;

```

Автоматический геттер для `myStruct` не будет:

```solidity
// Приблизительный пример, не реальный код
function myStruct() public view returns (MyStruct memory) {
    return myStruct;
}
```

Вместо этого он возвращает члены по отдельности:

```solidity
// Приблизительный пример, не реальный код
function myStruct() public view returns (uint, uint, uint) {
    return (myStruct.first, myStruct.second, myStruct.third);
}
```

Создайте свой собственный геттер, чтобы вернуть данные в виде кортежа, который будет интерпретирован как соответствующий тип, если он вызывается из другого контракта через интерфейс.

```solidity
function getMyStruct() public view returns (MyStruct memory) {
    return myStruct;
}
```

---

## Заключение

В этом уроке вы узнали, как использовать ключевое слово `struct` для создания пользовательского типа, который хранит связанные данные. Вы также узнали три метода создания их экземпляров и общие шаблоны для хранения `struct` в хранилище. Наконец, вы исследовали некоторые ограничения, которые возникают при работе с более сложными типами данных внутри `struct`.
