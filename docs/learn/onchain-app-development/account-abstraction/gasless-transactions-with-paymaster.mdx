# Бесплатные транзакции на Base с использованием Base Paymaster

Все еще пытаетесь привлечь пользователей в свое приложение? Хотите избавиться от забот о газе для транзакций и спонсировать их для своих пользователей на Base? Не ищите дальше!

Комиссии за транзакции на Base обычно составляют меньше цента, но концепция газа все еще может сбивать с толку новых пользователей. Вы можете абстрагироваться от этого и улучшить пользовательский опыт, используя **Base Paymaster**. Paymaster позволяет вам:

- Пакетировать многошаговые транзакции
- Создавать пользовательские бесплатные транзакции
- Спонсировать до $10k ежемесячно в мейннете (без ограничений в тестнете)

> **Примечание:** Если вам нужно увеличить лимит спонсирования, пожалуйста, [reach out on Discord][Discord]!

## Цели

1. Настроить меры безопасности для обеспечения безопасных и надежных транзакций.
2. Управлять и распределять ресурсы для спонсируемых транзакций.
3. Субсидировать комиссии за транзакции для пользователей, улучшая пользовательский опыт, делая транзакции бесплатными.
4. Настраивать и управлять спонсируемыми транзакциями по различным расписаниям, включая еженедельные, ежемесячные и ежедневные интервалы.

## Предварительные требования

Это руководство предполагает, что у вас есть:

1. **Аккаунт в Coinbase Cloud Developer Platform**  
   Если нет, зарегистрируйтесь на [CDP site]. После создания аккаунта вы сможете управлять проектами и использовать такие инструменты, как Paymaster.

2. **Знакомство со смарт-аккаунтами и ERC 4337**  
   Смарт-аккаунты являются основой продвинутых паттернов транзакций (например, пакетирование, спонсирование). Если вы новичок в ERC 4337, ознакомьтесь с внешними ресурсами, такими как официальный [EIP-4337 explainer](https://eips.ethereum.org/EIPS/eip-4337), прежде чем начать.

3. **Foundry**  
   Foundry - это среда разработки, фреймворк для тестирования и набор инструментов для смарт-контрактов Ethereum. Он понадобится вам локально для генерации пар ключей и взаимодействия со смарт-контрактами.

> **Testnet vs. Mainnet**  
> Если вы предпочитаете не тратить реальные средства, вы можете переключиться на **Base Goerli** (тестнет). Шаги ниже концептуально те же самые. Просто выберите _Base Goerli_ в Coinbase Developer Platform вместо _Base Mainnet_ и используйте контракт, развернутый в тестнете Base, для ваших разрешенных методов.

## Настройка Base Paymaster и Бандлера

В этом разделе вы настроите Paymaster для спонсирования платежей от имени определенного смарт-контракта на указанную сумму.

1. **Перейдите на [Coinbase Developer Platform].**
2. Создайте или выберите ваш проект в верхнем левом углу экрана.
3. Нажмите на инструмент **Paymaster** в левой навигации.
4. Перейдите на вкладку **Configuration** и скопируйте the **RPC URL** в буфер обмена - он скоро понадобится вам в коде.

### Скриншоты

- **Выбор вашего проекта**  
  ![cdp-home.png](/images/gasless-transaction-on-base/cdp-select-project.png)

- **Навигация к инструменту Paymaster**  
  ![cdp-paymaster-tool.png](/images/gasless-transaction-on-base/cdp-paymaster.png)

- **Экран конфигурации**  
  ![cdp-paymaster-tool.png](/images/gasless-transaction-on-base/cdp-config.png)

### Добавление разрешенного контракта для спонсирования

1. На странице конфигурации убедитесь, что выбран **Base Mainnet** (или **Base Goerli**, если вы тестируете).
2. Активируйте ваш paymaster, нажав переключатель.
3. Нажмите **Add** чтобы добавить разрешенный контракт.
4. Для этого примера добавьте [`0x83bd615eb93eE1336acA53e185b03B54fF4A17e8`][simple NFT contract], и функцию `mintTo(address)`.

![cdp-allowlist-contracts.png](/images/gasless-transaction-on-base/cdp-allowlist-contract.png)

> **Используйте свой собственный контракт**  
> Мы используем [simple NFT contract][simple NFT contract] в мейннете Base в качестве примера. Не стесняйтесь подставлять свой.

### Глобальные и пользовательские лимиты

Прокрутите вниз до раздела **Per User Limit**. Вы можете установить:

- **Лимит в долларах** или **количество UserOperations** на пользователя
- **Циклы лимита**, которые сбрасываются ежедневно, еженедельно или ежемесячно

Например, вы можете установить:

- `max USD` в `$0.05`
- `max UserOperation` в `1`

Это означает, что **каждый пользователь** может получить только $0.05 в спонсированном газе и **1** пользовательскую операцию до сброса цикла.

> **Циклы лимита (Limit Cycles)**  
> Они сбрасываются в соответствии с выбранным интервалом (ежедневно, еженедельно, ежемесячно).

Затем **Установите Глобальный лимит (Set the Global Limit)**. Например, установите его в $0.07, чтобы после того, как весь paymaster спонсировал газа на $0.07 (по всем пользователям), спонсирование прекратилось, пока вы не увеличите лимит.

![cdp-global-user-limits.png](/images/gasless-transaction-on-base/cdp-global-user-limits.png)

## Тестирование вашей политики Paymaster

Теперь давайте проверим, что эти политики работают. Мы:

1. Создадим две локальные пары ключей (или используем приватные ключи, которые у вас уже есть).
2. Сгенерируем два смарт-аккаунта.
3. Попытаемся спонсировать несколько транзакций, чтобы увидеть политику в действии.

### Установка Foundry

1. Убедитесь, что у вас установлен **Rust**. Если нет:
   ```bash Terminal
   curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
   ```
2. Установите Foundry:
   ```bash Terminal
   curl -L https://foundry.paradigm.xyz | bash
   foundryup
   ```
3. Убедитесь, что все работает:
   ```bash Terminal
   cast --help
   ```
   Если вы видите информацию об использовании Foundry, все в порядке!

### Создание проекта и генерация пар ключей

1. Создайте новую папку и установите зависимости:
   ```bash Terminal
   mkdir sponsored_transactions
   cd sponsored_transactions
   npm init es6
   npm install permissionless
   npm install viem
   touch index.js
   ```
2. Сгенерируйте две пары ключей с помощью Foundry:
   ```bash Terminal
   cast wallet new
   cast wallet new
   ```
   Вы увидите что-то вроде:
   ```bash Terminal
   Successfully created new keypair.
   Address: 0xD440D746...
   Private key: 0x01c9720c1dfa3c9...
   ```
   **Сохраните эти приватные ключи в надежном месте** - iжелательно в файле `.env`.

### Структура проекта с переменными окружения

Создайте файл `.env` в `sponsored_transactions`:

```bash Terminal
PAYMASTER_RPC_URL=https://api.developer.coinbase.com/rpc/v1/base/<SPECIAL-KEY>
PRIVATE_KEY_1=0x01c9720c1dfa3c9...
PRIVATE_KEY_2=0xbcd6fbc1dfa3c9...
```

> **Безопасность**  
> **Никогда** не добавляйте файлы `.env` в публичный репозиторий!

## Пример `index.js` (с использованием Twoslash)

Ниже приведен полный пример того, как вы можете структурировать `index.js`.  
Мы будем использовать блоки кода **twoslash** (````js twoslash`) для выделения ключевых строк и пояснений.

```js
// --- index.js ---
// @noErrors

// 1. Импорт модулей и переменных окружения
import 'dotenv/config';
import { http, createPublicClient, encodeFunctionData } from 'viem';
import { base } from 'viem/chains';
import { createSmartAccountClient } from 'permissionless';
import { privateKeyToSimpleSmartAccount } from 'permissionless/accounts';
import { createPimlicoPaymasterClient } from 'permissionless/clients/pimlico';

// 2. Получение секретов из .env
// Highlight: переменные окружения для paymaster, приватные ключи
const rpcUrl = process.env.PAYMASTER_RPC_URL; // highlight
const firstPrivateKey = process.env.PRIVATE_KEY_1; // highlight
const secondPrivateKey = process.env.PRIVATE_KEY_2; // highlight

// 3. Объявление адресов Base (точка входа и фабрика)
const baseEntryPoint = '0x5FF137D4b0FDCD49DcA30c7CF57E578a026d2789';
const baseFactoryAddress = '0x15Ba39375ee2Ab563E8873C8390be6f2E2F50232';

// 4. Создание публичного клиента для Base
const publicClient = createPublicClient({
  chain: base,
  transport: http(rpcUrl),
});

// 5. Настройка клиента Paymaster
const cloudPaymaster = createPimlicoPaymasterClient({
  chain: base,
  transport: http(rpcUrl),
  entryPoint: baseEntryPoint,
});

// 6. Создание смарт-аккаунтов из приватных ключей
async function initSmartAccounts() {
  const simpleAccount = await privateKeyToSimpleSmartAccount(publicClient, {
    privateKey: firstPrivateKey,
    factoryAddress: baseFactoryAddress,
    entryPoint: baseEntryPoint,
  });

  const simpleAccount2 = await privateKeyToSimpleSmartAccount(publicClient, {
    privateKey: secondPrivateKey,
    factoryAddress: baseFactoryAddress,
    entryPoint: baseEntryPoint,
  });

  // 7. Создание SmartAccountClient для каждого
  const smartAccountClient = createSmartAccountClient({
    account: simpleAccount,
    chain: base,
    bundlerTransport: http(rpcUrl),
    middleware: {
      sponsorUserOperation: cloudPaymaster.sponsorUserOperation,
    },
  });

  const smartAccountClient2 = createSmartAccountClient({
    account: simpleAccount2,
    chain: base,
    bundlerTransport: http(rpcUrl),
    middleware: {
      sponsorUserOperation: cloudPaymaster.sponsorUserOperation,
    },
  });

  return { smartAccountClient, smartAccountClient2 };
}

// 8. ABI для NFT-контракта
const nftAbi = [
  // ...
  // усечено для краткости
];

// 9. Пример функции для отправки транзакции из заданного SmartAccountClient
async function sendTransaction(client, recipientAddress) {
  try {
    // кодируем вызов функции "mintTo"
    const callData = encodeFunctionData({
      abi: nftAbi,
      functionName: 'mintTo',
      args: [recipientAddress], // highlight: указываем, кто получает отминтированный NFT
    });

    const txHash = await client.sendTransaction({
      account: client.account,
      to: '0x83bd615eb93eE1336acA53e185b03B54fF4A17e8', // адрес NFT-контракта
      data: callData,
      value: 0n,
    });

    console.log(`✅ Transaction successfully sponsored for ${client.account.address}`);
    console.log(`🔍 View on BaseScan: https://basescan.org/tx/${txHash}`);
  } catch (error) {
    console.error('Transaction failed:', error);
  }
}

// 10. Основной поток: инициализация аккаунтов, отправка транзакций
(async () => {
  const { smartAccountClient, smartAccountClient2 } = await initSmartAccounts();

  // Отправляем транзакцию от первого аккаунта
  await sendTransaction(smartAccountClient, smartAccountClient.account.address);

  // Отправляем транзакцию от второго аккаунта
  // Для разнообразия также минтаем на адрес второго аккаунта
  await sendTransaction(smartAccountClient2, smartAccountClient2.account.address);
})();
```

> **Примечание**:
>
> - Запустите это через `node index.js` из корня вашего проекта.
> - Если ваши настройки Paymaster строгие (например, лимит 1 транзакция на пользователя), при повторном запуске скрипта вы можете получить ошибку "request denied", что указывает на работу политики.

## Достижение лимитов политики и устранение неполадок

1. **Пользовательский лимит**  
   Если вы видите ошибку типа:

   ```json
   {
     "code": -32001,
     "message": "request denied - rejected due to maximum per address transaction count reached"
   }
   ```

   Это означает, что вы достигли вашего лимита **UserOperation** для одного аккаунта. Вернитесь в интерфейс , чтобы скорректировать политику.

2. **Глобальный лимит**  
   Если вы многократно запускаете транзакции и в итоге видите:
   ```json
   {
     "code": -32001,
     "message": "request denied - rejected due to max global usd Spend Permission reached"
   }
   ```
   Вы достигли **глобального** лимита спонсированного газа. Увеличьте его в дашборде CDP и подождите несколько минут, чтобы изменения вступили в силу.

## Проверка владения токеном (опционально)

Хотите убедиться, что токен действительно был отминтирован? Вы можете прочитать функцию `balanceOf` NFT:

```js
import { readContract } from 'viem'; // highlight

// пример функции
async function checkNftBalance(publicClient, contractAddress, abi, ownerAddress) {
  const balance = await publicClient.readContract({
    address: contractAddress,
    abi,
    functionName: 'balanceOf',
    args: [ownerAddress],
  });
  console.log(`NFT balance of ${ownerAddress} is now: ${balance}`);
}
```

## Заключение

В этом руководстве вы:

- Настроили и **скомфигурировали** Base Paymaster в Coinbase Developer Platform.
- **Добавили в Allowlist** контракт и конкретную функцию (`mintTo`) для спонсирования.
- Установили **пользовательские** и **глобальные лимиты** спонсирования для контроля затрат.
- Продемонстрировали **поток финансирования** со смарт-аккаунтами, используя `permissionless`, `viem`, и приватные ключи, сгенерированные Foundry.

Этот подход может значительно улучшить пользовательский опыт вашего dApp, устранив трение, связанное с газом. Для более сложных схем спонсирования (например, ежедневных или еженедельных циклов) просто настройте параметры пользовательских и глобальных лимитов в Coinbase Developer Platform.

> **Следующие шаги**
>
> - Используйте [proxy service][proxy service] для лучшей безопасности конечных точек.
> - Разверните свои собственные контракты и добавьте их в разрешенный список.
> - Экспериментируйте с объединением нескольких вызовов в одну спонсируемую транзакцию.

## Ссылки

- [list of factory addresses]
- [Discord]
- [CDP site]
- [Coinbase Developer Platform]
- [UI]
- [proxy service]
- [Paymaster Tool]
- [Foundry Book installation guide]
- [simple NFT contract]

[list of factory addresses]: https://docs.alchemy.com/reference/factory-addresses
[Discord]: https://discord.com/invite/cdp
[CDP site]: https://portal.cdp.coinbase.com/
[Coinbase Developer Platform]: https://portal.cdp.coinbase.com/
[UI]: https://portal.cdp.coinbase.com/products/bundler-and-paymaster
[proxy service]: https://www.smartwallet.dev/guides/paymasters
[Paymaster Tool]: https://portal.cdp.coinbase.com/products/bundler-and-paymaster
[Foundry Book installation guide]: https://book.getfoundry.sh/getting-started/installation
[simple NFT contract]: https://basescan.org/token/0x83bd615eb93ee1336aca53e185b03b54ff4a17e8

**Happy Building on Base!**
