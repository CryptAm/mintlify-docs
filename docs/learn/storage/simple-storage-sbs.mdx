---
title: Хранение данных
sidebarTitle: Пошаговое руководство
description: Узнайте, как хранить данные в блокчейне.
hide_table_of_contents: false
---

В конечном счете, сила блокчейна заключается в том, что любой может хранить свои данные в нем через `storage` в смарт-контракте. В этом пошаговом руководстве вы узнаете, как получить доступ и использовать расположение данных `storage`.

---

## Цели

К концу этого урока вы должны уметь:

- Использовать конструктор для инициализации переменной
- Получать доступ к данным в публичной переменной с помощью автоматически созданного геттера
- Упорядочивать объявления переменных для эффективного использования хранилища

---

## Простой контракт для хранения

Создайте контракт с именем `SimpleStorage`.

### Добавление переменной в storage

В Solidity переменные, объявленные на уровне класса, автоматически являются переменными `storage`. Создайте переменную для хранения возраста человека и другую для хранения количества автомобилей, которыми он владеет. Присвойте `age` начальное значение по вашему выбору, но не присваивайте значение `cars`;

<Accordion title="Показать код">

```solidity
contract SimpleStorage {
    uint8 age = 41;
    uint8 cars;
}
```

</Accordion>

Поскольку возраст человека или количество автомобилей вряд ли превысит 255, мы можем использовать `uint8` для каждой из этих переменных. Для типов, размер которых меньше 32 байт, несколько переменных одного типа будут [packed] в один слот хранилища. Для этого переменные должны быть объявлены вместе.

```solidity
// Эти переменные используют преимущества упаковки
uint8 first;
uint8 second;
uint third;

// Эти переменные НЕ используют преимущества упаковки и должны быть переупорядочены
uint8 fourth;
uint fifth;
uint8 sixth;
```

### Инициализация значения с помощью конструктора

Вы можете добавить функцию `constructor` в ваш контракт. Как и в других языках, эта функция вызывается ровно один раз при развертывании контракта. Конструктор может иметь параметры, но они не обязательны.

Вы можете использовать конструктор для выполнения различных задач настройки. Например, конструктор токена _ERC-721_, который является основным механизмом для большинства NFT, использует конструктор для установки имени и символа токена.

Создайте конструктор и используйте его, чтобы присвоить значение по вашему выбору переменной `cars`.

<Accordion title="Показать код">

```solidity
constructor() {
    cars = 1;
}
```

</Accordion>

### Доступ к переменным состояния

Разверните ваш контракт в Remix. Он должен работать нормально, но у вас будет одна проблема: нет способа проверить, имеют ли переменные ожидаемые значения!

Эту проблему можно решить, написав функции, которые возвращают значения ваших переменных состояния, но в этом нет необходимости. Компилятор Solidity автоматически создает геттеры для всех `public` переменных.

Добавьте ключевое слово `public` к обеим переменным. В отличие от большинства языков, `public` ставится после объявления типа. Ваш контракт теперь должен быть похож на:

<Accordion title="Показать код">

```solidity
contract SimpleStorage {
    uint8 public age = 41;
    uint8 public cars;
    constructor() {
        cars = 1;
    }
}
```

</Accordion>

Повторно разверните ваш контракт и проверьте, чтобы убедиться.

---

## Установка переменной состояния с помощью функции

Хорошие новости! Наш пользователь купил второй автомобиль! Единственная проблема в том, что у нас нет способа обновить количество хранимых `cars`.

### Добавление функции для обновления `cars`

Прежде чем писать функцию, давайте подумаем о соображениях проектирования для этой функциональности. В любой момент пользователь может:

- Купить или иным образом приобрести новый автомобиль
- Получить несколько новых автомобилей сразу (Ура!)
- Продать или отдать один или несколько автомобилей (😞)

Учитывая такое разнообразие условий, **хорошим** подходом было бы обрабатывать вычисление правильного количества автомобилей на фронтенде и передавать обновленное значение в бэкенд.

Чтобы удовлетворить эту потребность, мы можем написать `public` функцию, которая принимает `uint8` для `_numberOfCars` и просто присваивает это значение переменной состояния `cars`. Поскольку эта функция изменяет состояние, ей не нужны модификаторы `pure` или `view`. Она не является ни тем, ни другим.

<Accordion title="Показать код">

```solidity
function updateNumberOfCars(uint8 _numberOfCars) public {
    cars = _numberOfCars;
}
```

</Accordion>

Разверните и протестируйте, чтобы убедиться, что она работает как ожидалось.

<Warning>
Хотя упаковка переменных может сэкономить газ, она также может увеличить его расход. EVM работает с данными по 32 байта за раз, поэтому потребуются дополнительные шаги для уменьшения размера элемента перед хранением.
Кроме того, экономия при записи в хранилище применяется только при одновременной записи нескольких значений в один слот.

Ознакомьтесь с **Предупреждением** в разделе документации [layout] о для получения более подробной информации!
</Warning>


### Добавление функции для обновления `age`

Было бы также хорошо иметь возможность обновлять значение `age`. Эта проблема имеет несколько иные соображения. К сожалению, `age` никогда не уменьшается. Он, вероятно, должен увеличиваться только на один год при каждом обновлении. Оператор `++` работает в Solidity, поэтому мы можем использовать его для создания функции, которая просто увеличивает возраст при вызове.

<Accordion title="Показать код">


```solidity
function increaseAge() public {
    age++;
}
```

</Accordion>


Но что, если пользователь вызовет эту функцию по ошибке? Хороший вопрос!

Самостоятельно добавьте функцию `adminSetAge`, которая может устанавливать `age` в указанное значение.

### Рефакторинг конструктора для приема аргументов

У нас остается одна проблема с этим контрактом. Что, если у вашего пользователя другой `age` или количество `cars`, чем захардкожено в контракте?

Как упоминалось выше, `constructor` **может** принимать аргументы и использовать их при развертывании. Давайте переработаем контракт, чтобы установить две переменные состояния в конструкторе на основе предоставленных значений.

<Accordion title="Показать код">

```solidity
contract SimpleStorage {
    uint8 public age;
    uint8 public cars;
    constructor(uint8 _age, uint8 _cars) {
        age = _age;
        cars = _cars;
    }
}
```

</Accordion>

Повторно разверните ваш контракт. Обратите внимание, что теперь, когда вы добавили параметры в `constructor`, вам нужно будет предоставить их при развертывании.

<Frame>
![Deployment](/images/learn/storage/deployment-with-params.png)
</Frame>

После завершения ваш контракт должен быть похож на:


<Accordion title="Показать код">

```solidity
contract SimpleStorage {
    uint8 public age;
    uint8 public cars;
    constructor(uint8 _age, uint8 _cars) {
        age = _age;
        cars = _cars;
    }

    function updateNumberOfCars(uint8 _numberOfCars) public {
        cars = _numberOfCars;
    }

    function increaseAge() public {
        age++;
    }

    function adminSetAge(uint8 _age) public {
        age = _age;
    }
}
```

</Accordion>

---

## Заключение

В этом уроке вы изучили, как постоянно хранить значения в блокчейне. Вы также попрактиковались в их обновлении из функций. Наконец, вы узнали, как использовать конструктор для выполнения функций настройки при развертывании, с параметрами и без них.

---

[packed]: https://docs.soliditylang.org/en/v0.8.17/internals/layout_in_storage.html
[layout]: https://docs.soliditylang.org/en/v0.8.17/internals/layout_in_storage.html
