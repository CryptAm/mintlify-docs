---
sidebarTitle: Бесплатные транзакции
title: 'Бесплатные транзакции (Gasless) на Base с использованием Paymaster'
description: Узнайте, как использовать Base Paymaster для бесшовных, бесплатных (gasless) транзакций на платформе Coinbase Cloud Developer Platform.
---

import { Danger } from "/snippets/danger.mdx";

Комиссии за транзакции на Base обычно составляют меньше цента, но сама концепция газа (gas) всё ещё может сбивать с толку новых пользователей и приводить к плохому пользовательскому опыту, когда у пользователей нет средств на газ в их кошельке. Вы можете абстрагироваться от этого и улучшить UX, используя **Base Paymaster**. Paymaster позволяет вам:

- Объединять многошаговые транзакции в пакеты
- Создавать пользовательские бесплатные (gasless) сценарии
- Спонсировать до $15k ежемесячно в основной сети (без ограничений в тестовой сети)

<Tip>
Если вам нужно увеличить лимит спонсирования, пожалуйста, [свяжитесь с нами в Discord][Discord]!
</Tip>


## Цели

1. Настроить меры безопасности для обеспечения безопасных и надежных транзакций.
2. Управлять и распределять ресурсы для спонсируемых транзакций.
3. Субсидировать комиссии за транзакции для пользователей, улучшая пользовательский опыт, делая транзакции бесплатными.
4. Настроить и управлять спонсируемыми транзакциями по различным расписаниям, включая еженедельные, ежемесячные и ежедневные интервалы.

## Предварительные требования

Это руководство предполагает, что у вас есть:

1. **Аккаунт на платформе Coinbase Cloud Developer Platform**  
   Если нет, зарегистрируйтесь на [CDP site]. После получения аккаунта вы сможете управлять проектами и использовать такие инструменты, как Paymaster.

2. **Знакомство со смарт-аккаунтами и ERC 4337**  
   Смарт-аккаунты (Smart Accounts) являются основой продвинутых паттернов транзакций (например, объединение в пакеты, спонсирование). Если вы новичок в ERC 4337, ознакомьтесь с внешними ресурсами, такими как официальный [объяснитель EIP-4337](https://eips.ethereum.org/EIPS/eip-4337) прежде чем начать.

3. **Foundry**  
   Foundry - это среда разработки, фреймворк для тестирования и набор инструментов для смарт-контрактов Ethereum. Он понадобится вам локально для генерации пар ключей и взаимодействия со смарт-контрактами.

<Tip>
**Тестовая сеть vs основная сеть**  
Если вы предпочитаете не тратить реальные средства, вы можете переключиться на **Base Sepolia** (тестовая сеть). Шаги ниже концептуально одинаковы. Просто выберите _Base Sepolia_ в Coinbase Developer Platform вместо _Base Mainnet_ и используйте контракт, развернутый в тестовой сети Base, для ваших разрешенных методов.
</Tip>


## Настройте Base Paymaster & Bundler

В этом разделе вы настроите Paymaster для спонсирования платежей от имени определенного смарт-контракта на указанную сумму.

1. **Перейдите на [Coinbase Developer Platform].**
2. Создайте или выберите ваш проект в левом верхнем углу экрана.
3. Нажмите на инструмент **Paymaster** в левой навигации.
4. Перейдите на вкладку **Configuration** и скопируйте **RPC URL** в буфер обмена - он скоро понадобится вам в коде.

### Скриншоты

- **Выбор вашего проекта**  
<Frame>
  ![cdp-home.png](/images/gasless-transaction-on-base/cdp-select-project.png)
</Frame>

- **Переход к инструменту Paymaster**  
<Frame>
  ![cdp-paymaster-tool.png](/images/gasless-transaction-on-base/cdp-paymaster.png)
</Frame>

- **Экран конфигурации**  
<Frame>
  ![cdp-paymaster-tool.png](/images/gasless-transaction-on-base/cdp-config.png)
</Frame>

### Добавьте разрешенный контракт (Allowlist)

1. На странице Configuration убедитесь, что выбрана **Base Mainnet** (или **Base Sepolia**, если вы тестируете).
2. Включите ваш paymaster, нажав на переключатель.
3. Нажмите **Add**, чтобы добавить разрешенный контракт.
4. Для этого примера добавьте [`0x83bd615eb93eE1336acA53e185b03B54fF4A17e8`][simple NFT contract] и функцию `mintTo(address)`.

<Frame>
![cdp-allowlist-contracts.png](/images/gasless-transaction-on-base/cdp-allowlist-contract.png)
</Frame>

<Note>
**Используйте свой собственный контракт**  
 Мы используем [простой NFT контракт][simple NFT contract] в основной сети Base в качестве примера. Не стесняйтесь использовать свой собственный.
</Note>


### Глобальные и пользовательские лимиты

Прокрутите вниз до раздела **Per User Limit**. Вы можете установить:

- **Лимит в долларах** или количество **UserOperations** на пользователя
- **Циклы лимитов**, которые сбрасываются ежедневно, еженедельно или ежемесячно

Например, вы можете установить:

- `max USD` в `$0.05`
- `max UserOperation` в `1`

Это означает, что **каждый пользователь** может получить только $0.05 спонсированного газа и **1** пользовательскую операцию (UserOperation) до сброса цикла.

<Note>
**Циклы лимитов** 
Они сбрасываются в зависимости от выбранного интервала (ежедневно, еженедельно, ежемесячно).
</Note>


Далее **установите Глобальный лимит**. Например, установите его на $0.07, чтобы как только весь paymaster спонсировал газа на $0.07 (все пользователи вместе), спонсирование прекращалось, пока вы не увеличите лимит.

<Frame>
![cdp-global-user-limits.png](/images/gasless-transaction-on-base/cdp-global-user-limits.png)
</Frame>

## Протестируйте вашу политику Paymaster

Теперь давайте проверим, работают ли эти политики. Мы:

1. Создадим две локальные пары ключей (или используем ваши собственные приватные ключи).
2. Сгенерируем два смарт-аккаунта.
3. Попытаемся спонсировать несколько транзакций, чтобы увидеть политику в действии.

### Установка Foundry

1. Убедитесь, что **Rust** установлен
   ```bash
   curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
   ```
2. Установите Foundry
   ```bash
   curl -L https://foundry.paradigm.xyz | bash
   foundryup
   ```
3. Проверьте, что все работает
   ```bash
   cast --help
   ```
   Если вы видите информацию об использовании Foundry, всё в порядке!

### Создайте ваш проект и сгенерируйте пары ключей

1. Создайте новую папку и установите зависимости, `viem` и `permissionless`:
   ```bash
   mkdir sponsored_transactions
   cd sponsored_transactions
   npm init es6
   npm install permissionless
   npm install viem
   touch index.js
   ```
2. Сгенерируйте две пары ключей с помощью Foundry:
   ```bash
   cast wallet new
   cast wallet new
   ```
   Вы увидите что-то вроде:
   ```bash
   Successfully created new keypair.
   Address: 0xD440D746...
   Private key: 0x01c9720c1dfa3c9...
   ```
   **Сохраните эти приватные ключи в безопасном месте**

### Структура проекта с переменными окружения

Создайте файл `.env` в директории `sponsored_transactions`. В `.env` добавьте rpcURL для вашего paymaster и приватные ключи для ваших аккаунтов:

<Info>
**Найдите endpoint для вашего Paymaster & Bundler*

Paymaster & Bundler endpoint - это URL для вашего Paymaster в Coinbase Developer Platform (CDP).
Он был сохранен в предыдущем разделе и имеет следующий формат: `https://api.developer.coinbase.com/rpc/v1/base/<SPECIAL-KEY>`
Перейдите в [Paymaster Tool] и выберите `Configuration` в верхней части экрана, чтобы получить ваш RPC URL.
</Info>


<Danger>
**Защитите ваши endpoints**

Вы создадите константу для endpoint'а Paymaster & Bundler, полученного с cdp.portal.coinbase.com. Самый безопасный способ сделать это - использовать прокси. Для целей этой демонстрации мы захардкодим его в наш файл `index.js`. Для продакшена мы настоятельно рекомендуем использовать [proxy service].
</Danger>


```bash
PAYMASTER_RPC_URL=https://api.developer.coinbase.com/rpc/v1/base/<SPECIAL-KEY>
PRIVATE_KEY_1=0x01c9720c1dfa3c9...
PRIVATE_KEY_2=0xbcd6fbc1dfa3c9...
```

<Danger>
Никогда не коммитьте файлы `.env` в публичный репозиторий!
</Danger>


## Пример `index.js`

Ниже приведен полный пример структуры `index.js`.

```js index.js
// --- index.js ---
// @noErrors

// 1. Импортируем модули и переменные окружения
import 'dotenv/config';
import { http, createPublicClient, encodeFunctionData } from 'viem';
import { base } from 'viem/chains';
import { createSmartAccountClient } from 'permissionless';
import { privateKeyToSimpleSmartAccount } from 'permissionless/accounts';
import { createPimlicoPaymasterClient } from 'permissionless/clients/pimlico';

// 2. Получаем секреты из .env
// Выделение: переменные окружения для paymaster, приватные ключи
const rpcUrl = process.env.PAYMASTER_RPC_URL; // highlight
const firstPrivateKey = process.env.PRIVATE_KEY_1; // highlight
const secondPrivateKey = process.env.PRIVATE_KEY_2; // highlight

// 3. Объявляем адреса Base (entrypoint и фабрика)
const baseEntryPoint = '0x5FF137D4b0FDCD49DcA30c7CF57E578a026d2789';
const baseFactoryAddress = '0x15Ba39375ee2Ab563E8873C8390be6f2E2F50232';

// 4. Создаем публичного клиента для Base
const publicClient = createPublicClient({
  chain: base,
  transport: http(rpcUrl),
});

// 5. Настраиваем клиент Paymaster
const cloudPaymaster = createPimlicoPaymasterClient({
  chain: base,
  transport: http(rpcUrl),
  entryPoint: baseEntryPoint,
});

// 6. Создаем смарт-аккаунты из приватных ключей
async function initSmartAccounts() {
  const simpleAccount = await privateKeyToSimpleSmartAccount(publicClient, {
    privateKey: firstPrivateKey,
    factoryAddress: baseFactoryAddress,
    entryPoint: baseEntryPoint,
  });

  const simpleAccount2 = await privateKeyToSimpleSmartAccount(publicClient, {
    privateKey: secondPrivateKey,
    factoryAddress: baseFactoryAddress,
    entryPoint: baseEntryPoint,
  });

  // 7. Создаем SmartAccountClient для каждого
  const smartAccountClient = createSmartAccountClient({
    account: simpleAccount,
    chain: base,
    bundlerTransport: http(rpcUrl),
    middleware: {
      sponsorUserOperation: cloudPaymaster.sponsorUserOperation,
    },
  });

  const smartAccountClient2 = createSmartAccountClient({
    account: simpleAccount2,
    chain: base,
    bundlerTransport: http(rpcUrl),
    middleware: {
      sponsorUserOperation: cloudPaymaster.sponsorUserOperation,
    },
  });

  return { smartAccountClient, smartAccountClient2 };
}

// 8. ABI для NFT контракта
const nftAbi = [
  // ...
  // сокращено для краткости
];

// 9. Пример функции для отправки транзакции из заданного SmartAccountClient
async function sendTransaction(client, recipientAddress) {
  try {
    // кодируем вызов функции "mintTo"
    const callData = encodeFunctionData({
      abi: nftAbi,
      functionName: 'mintTo',
      args: [recipientAddress], // highlight: указываем, кто получит отчеканенный NFT
    });

    const txHash = await client.sendTransaction({
      account: client.account,
      to: '0x83bd615eb93eE1336acA53e185b03B54fF4A17e8', // адрес NFT контракта
      data: callData,
      value: 0n,
    });

    console.log(`✅ Transaction successfully sponsored for ${client.account.address}`);
    console.log(`🔍 View on BaseScan: https://basescan.org/tx/${txHash}`);
  } catch (error) {
    console.error('Transaction failed:', error);
  }
}

// 10. Основной поток: инициализируем аккаунты, отправляем транзакции
(async () => {
  const { smartAccountClient, smartAccountClient2 } = await initSmartAccounts();

  // Отправляем транзакцию с первого аккаунта
  await sendTransaction(smartAccountClient, smartAccountClient.account.address);

  // Отправляем транзакцию со второго аккаунта
  // Для разнообразия также отчеканим на адрес второго аккаунта
  await sendTransaction(smartAccountClient2, smartAccountClient2.account.address);
})();
```

Теперь, когда код реализован, давайте запустим его:
Запустите через `node index.js` из корня вашего проекта.

```bash
node index.js
```

Вы должны увидеть вывод "Transaction successfully sponsored".

Чтобы подтвердить, что ваши политики расходов корректно настроены, попробуйте запустить скрипт снова. Если настройки вашего Paymaster строгие (например, лимит 1 транзакция на пользователя), при втором запуске скрипта вы можете получить ошибку "request denied", что указывает на работу политики.

## Достижение лимитов политики и устранение проблем

1. **Пользовательский лимит**  
   Если вы видите ошибку типа:

   ```json
   {
     "code": -32001,
     "message": "request denied - rejected due to maximum per address transaction count reached"
   }
   ```

   Это означает, что вы достигли лимита **UserOperation** для одного аккаунта. Вернитесь в UI , чтобы скорректировать политику.

2. **Глобальный лимит**  
   Если вы многократно запускаете транзакции и в конце концов видите:
   ```json
   {
     "code": -32001,
     "message": "request denied - rejected due to max global usd Spend Permission reached"
   }
   ```
   Вы достигли **глобального** лимита спонсированного газа. Увеличьте его в дашборде CDP и подождите несколько минут, чтобы изменения вступили в силу.

## Проверка владения токеном (опционально)

Хотите подтвердить, что токен действительно был отчеканен? Вы можете прочитать функцию `balanceOf` NFT:

```js
import { readContract } from 'viem'; // highlight

// пример функции
async function checkNftBalance(publicClient, contractAddress, abi, ownerAddress) {
  const balance = await publicClient.readContract({
    address: contractAddress,
    abi,
    functionName: 'balanceOf',
    args: [ownerAddress],
  });
  console.log(`NFT balance of ${ownerAddress} is now: ${balance}`);
}
```

## Заключение

В этом руководстве вы:

- Настроили и **сконфигурировали** Base Paymaster на платформе Coinbase Developer Platform.
- **Добавили в разрешенный список (allowlisted)** контракт и конкретную функцию (`mintTo`) для спонсирования.
- Установили **пользовательские** и **глобальные лимиты** спонсирования для контроля расходов.
- Продемонстрировали **поток спонсирования** со смарт-аккаунтами, используя `permissionless`, `viem` и приватные ключи, сгенерированные Foundry.

Этот подход может значительно улучшить пользовательский опыт вашего dApp, устраняя трение, связанное с газом. Для более сложных схем спонсирования (например, ежедневных или еженедельных циклов) просто настройте параметры пользовательских и глобальных лимитов в Coinbase Developer Platform.

> **Следующие шаги**
>
> - Используйте [сервис прокси][proxy service] для лучшей безопасности endpoint.
> - Разверните свои собственные контракты и добавьте их в разрешенный список.
> - Экспериментируйте с объединением нескольких вызовов в одну спонсируемую транзакцию.

## Ссылки

- [список адресов фабрик]
- [Discord]
- [сайт CDP]
- [Coinbase Developer Platform]
- [UI]
- [сервис прокси]
- [Paymaster Tool]
- [Foundry Book installation guide]
- [простой NFT контракт]

[список адресов фабрик]: https://docs.alchemy.com/reference/factory-addresses
[Discord]: https://discord.com/invite/buildonbase
[сайт CDP]: https://portal.cdp.coinbase.com/
[Coinbase Developer Platform]: https://portal.cdp.coinbase.com/
[UI]: https://portal.cdp.coinbase.com/products/bundler-and-paymaster
[сервис прокси]: https://www.smartwallet.dev/guides/paymasters
[Paymaster Tool]: https://portal.cdp.coinbase.com/products/bundler-and-paymaster
[Руководство по установке Foundry Book]: https://book.getfoundry.sh/getting-started/installation
[простой NFT контракт]: https://basescan.org/token/0x83bd615eb93ee1336aca53e185b03b54ff4a17e8

**Happy Building on Base!**
