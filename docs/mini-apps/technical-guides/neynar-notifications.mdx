---
title: Отправка уведомлений (Neynar)
description: Узнайте, как вовлекать пользователей с помощью внутриприложенных уведомлений, используя Neynar
---


## Обзор


Если у вашего приложения нет backend-части, если вы уже используете Neynar или предпочитаете использовать хостируемое решение вместо [реализации логики уведомлений](/mini-apps/core-concepts/notifications) в вашем бэкенде, мы рекомендуем использовать Neynar для уведомлений. [**Neynar**](https://neynar.com/) - это инфраструктурная платформа для создания мини-приложений на Base. 

Решение Neynar позволяет вам:

- Управлять инфраструктурой уведомлений (токены, разрешения, пакетирование) в едином интерфейсе
- Отправлять через API или UI портала разработчика Neynar - без необходимости писать код
- Отслеживать производительность уведомлений с помощью встроенной аналитики
- Таргетировать конкретные когорты пользователей с помощью расширенной сегментации


## Предварительные требования
- Аккаунт в приложении Base
- Мини-приложение с реализованным [Farcaster SDK](https://miniapps.farcaster.xyz/docs/getting-started#manual-setup)
- Аккаунт разработчика Neynar -- зарегистрируйтесь бесплатно [здесь](https://neynar.com)


## Включение уведомлений

<Steps>
<Step title="Настройте уведомления в Neynar">
URL вебхука событий мини-приложения Neynar находится на странице приложения в Neynar.

Перейдите на [dev.neynar.com/app](https://dev.neynar.com/app) и нажмите на ваше приложение.

Скопируйте URL в разделе **Mini** app **Notifications**.

![neynar webhook url](/images/miniapps/neynar-notification-webhook.png)


</Step>
<Step title="Добавьте URL вебхука в манифест">
Вставьте URL, скопированный из поля **Mini app Notifications** на предыдущем шаге, в поле `webhookUrl` внутри объекта `miniapp` в вашем манифесте.

Вот пример манифеста с обновленным полем `webhookUrl`:
```json highlight={14}
{
  "accountAssociation": {
    "header": "eyJmaWQiOjE5MSwidHlwZSI6ImN1c3RvZHkiLCJrZXkiOiIweDNhNmRkNTY5ZEU4NEM5MTgyOEZjNDJEQ0UyMGY1QjgyN0UwRUY1QzUifQ",
    "payload": "eyJkb21haW4iOiIxYmNlLTczLTcwLTE2OC0yMDUubmdyb2stZnJlZS5hcHAifQ",
    "signature": "MHg1ZDU1MzFiZWQwNGZjYTc5NjllNDIzNmY1OTY0ZGU1NDMwNjE1YTdkOTE3OWNhZjE1YjQ5M2MxYWQyNWUzMTIyM2NkMmViNWQyMjFhZjkxYTYzM2NkNWU3NDczNmQzYmE4NjI4MmFiMTU4Y2JhNGY0ZWRkOTQ3ODlkNmM2OTJlNDFi"
  },
  "miniapp": {
    "version": "4.2.0",
    "name": "Your Mini App Name",
    "iconUrl": "https://your-miniapp-domain.com/icon.png",
    "splashImageUrl": "https://your-miniapp-domain.com/splash.png",
    "splashBackgroundColor": "#f7f7f7",
    "homeUrl": "https://your-miniapp-domain.com",
    "webhookUrl": "https://api.neynar.com/f/app/3c274018-6402-4a47-96bbg-1fea72afc408/event"
  }
}
```

<Warning>
Кэширование: Приложение Base может кэшировать манифест вашего мини-приложения. Чтобы убедиться, что все изменения вступили в силу, перепостите ваше приложение в Base App.
</Warning>

<Tip>
Протестируйте ваше мини-приложение в [Base Build](https://base.dev/preview) с помощью инструмента Preview. После входа вставьте URL вашего приложения в поле `App URL` и нажмите кнопку `Submit`.
</Tip>

</Step>
</Steps>

## Предложение пользователям добавить ваше приложение
Чтобы отправлять пользователям уведомления, они сначала должны добавить ваше приложение.
<Steps>
<Step title="Установите React-пакет Neynar">


```shell terminal
npm install @neynar/react
```
</Step>


<Step title="Предложите пользователям добавить ваше мини-приложение">

Используйте хук `addMiniApp()`, чтобы предложить пользователям добавить ваше мини-приложение.

Neynar будет управлять всеми [событиями уведомлений](/mini-apps/core-concepts/notifications#events) которые сообщают вашему приложению, когда пользователь включил или отключил уведомления для вашего мини-приложения.

Чтобы подтвердить, что мини-приложение было добавлено и уведомления включены, проверьте, что `result.added` равно `true` и `result.notificationDetails` является валидным объектом.

```javascript app/page.tsx
import { useMiniApp } from '@neynar/react';

export default function HomePage() {
  const { isSDKLoaded, addMiniApp } = useMiniApp();

  const handleAddMiniApp = async () => {
    if (!isSDKLoaded) return;
    
    const result = await addMiniApp();
    if (result.added && result.notificationDetails) {
      // Мини-приложение было добавлено и уведомления были включены
      console.log('Notification token:', result.notificationDetails.token);
    }
  };

  return (
    <button onClick={handleAddMiniApp}>
      Add Mini App
    </button>
  );
}
```

</Step>
</Steps>

## Отправка уведомлений пользователям

Neynar упрощает отправку уведомлений всем пользователям, включившим уведомления, или их подмножеству на основе заданных вами критериев фильтрации.

### Вариант 1: API

Вы можете программно отправлять уведомления с помощью API Neynar. Это дает вам полный контроль над таргетингом и фильтрацией пользователей.

<Steps>
<Step title="Установите Node.js SDK Neynar">

Установите пакет [@neynar/nodejs-sdk](https://github.com/neynarxyz/nodejs-sdk):

```shell
npm install @neynar/nodejs-sdk
```
</Step>

<Step title="Создайте функцию отправки уведомлений">

Создайте переиспользуемую функцию для отправки уведомлений с возможностями таргетинга и фильтрации:

```javascript lib/sendNotification.js
import { NeynarAPIClient } from "@neynar/nodejs-sdk";

const client = new NeynarAPIClient(process.env.NEYNAR_API_KEY);

/**
 * Отправить уведомление пользователям мини-приложения
 * @param {number[]} targetFids - Массив FID для таргетинга (пустой массив = все пользователи с включенными уведомлениями)
 * @param {Object} filters - Опциональные фильтры для сужения круга получателей
 * @param {Object} notification - Содержимое уведомления и целевой URL
 */
export async function sendNotification(targetFids, filters, notification) {
  try {
    const response = await client.publishFrameNotifications({
      targetFids,
      filters,
      notification,
    });
    
    return {
      success: true,
      data: response,
    };
  } catch (error) {
    console.error("Failed to send notification:", error);
    return {
      success: false,
      error: error.message,
    };
  }
}
```
</Step>

<Step title="Отправьте уведомления">

Используйте функцию для массовой рассылки уведомлений с расширенными критериями фильтрации:

```javascript sendNotificationsAdvanced.ts
import { sendNotification } from './lib/sendNotification';

// Определите целевые FID (пустой массив таргетирует всех пользователей с включенными уведомлениями)
const targetFids = [];

// Определите фильтры для сужения круга получателей
const filters = {
  exclude_fids: [420, 69], // Исключить конкретные FID
  following_fid: 3, // Отправлять только пользователям, подписанным на этот FID
  minimum_user_score: 0.5, // Отправлять только пользователям с оценкой >= этого значения
  near_location: { // Отправлять только пользователям рядом с определенным местоположением
    latitude: 34.052235,
    longitude: -118.243683,
    radius: 50000, // Расстояние в метрах (опционально, по умолчанию 50 км)
  }
};

// Определите содержимое уведомления
const notification = {
  title: "🪐",
  body: "It's time to savor farcaster",
  target_url: "https://your-miniapp-domain.com/notification-destination",
};

// Отправьте уведомление
const result = await sendNotification(targetFids, filters, notification);

if (result.success) {
  console.log("Notification sent successfully:", result.data);
} else {
  console.error("Failed to send notification:", result.error);
}
```

<Tip>
Параметр `target_fids` является отправной точкой для всей фильтрации. Передайте пустой массив, чтобы таргетировать всех пользователей с включенными уведомлениями, или укажите конкретные FID для таргетинга определенных пользователей.
</Tip>
</Step>
</Steps>

### Вариант 2: Интерфейс (UI) Neynar

[Портал разработчика Neynar](https://dev.neynar.com) предлагает те же функции, что и API, для массовой рассылки уведомлений. Перейдите в ваше приложение и нажмите вкладку "Mini App". После настройки вашего мини-приложения с URL вебхука Neynar и включения пользователями уведомлений для вашего мини-приложения, вы увидите раздел "Broadcast Notification" с раскрывающимся разделом фильтров.


<Steps>
<Step title="Войдите в портал разработчика Neynar">
https://dev.neynar.com/home
</Step>

<Step title="Выберите целевое приложение">
![select app in neynar dev portal](/images/miniapps/neynar-select-app.png)

</Step>
<Step title="Перейдите на вкладку Mini App">
![select app in neynar dev portal](/images/miniapps/neynar-mini-app-tab.png)
</Step>

<Step title="Заполните детали уведомления">
![select app in neynar dev portal](/images/miniapps/neynar-send-notification.png)
<Tip>Параметр `target_fids` является отправной точкой для всей фильтрации. Передайте пустой массив для `target_fids`, чтобы начать с набора всех FID с включенными уведомлениями для вашего приложения, или вручную определите `target_fids` для списка конкретных FID.</Tip>

</Step>

<Step title="Нажмите кнопку Broadcast">
После заполнения деталей уведомления и применения любых фильтров запустите рассылку вашего уведомления, нажав кнопку broadcast внизу страницы.
</Step>
</Steps>




Дополнительная документация по API и его параметрам тела запроса доступна по ссылке [publish-miniapp-notifications](https://docs.neynar.com/reference/publish-frame-notifications).
